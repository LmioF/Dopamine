# Dopamine 3 roothide port review

Review started: 2026-09-14 (Europe/Zurich).

Status: the four-way source-level port inventory is complete for the pinned uploaded snapshots and the subsystem scope in section 7. The implementation phase was authorized on 2026-09-14 and is now in progress in the same worktree, entirely through Codexify. Historical review observations below remain baseline evidence, not fresh verification of fixes. The report is not a claim that every possible bug has been found or that the port is ready for release.

### Implementation tracking

Each numbered finding has one resolution checkbox. It remains unchecked until all of that finding's actionable subissues are fixed and the stated verification is complete. Partial fixes, host-only verification and outstanding device/private-ABI obligations are recorded next to the checkbox rather than silently counted as complete. The section 6 validation matrix remains separate from source-level test results. Existing Wolf identity/testing overrides and unrelated working-tree changes must be preserved. No local ChatGPT sandbox source or report copy is used for this phase.

#### Progress snapshot - 2026-09-17

| Tracking state | Count |
| --- | ---: |
| Findings with a recorded resolution | 49 |
| Open findings with implementation or fixture work recorded | 0 |
| Open findings still marked pending | 0 |
| Total numbered findings | 49 |
| Separate validation gaps, all open | 10 |

The recorded resolutions are PR-001, PR-002, PR-003, PR-004, PR-005, PR-006, PR-007, PR-008, PR-009, PR-010, PR-011, PR-012, PR-013, PR-014, PR-015, PR-016, PR-017, PR-018, PR-019, PR-020, PR-021, PR-022, PR-023, PR-024, PR-025, PR-026, PR-027, PR-028, PR-029, PR-030, PR-031, PR-032, PR-033, PR-034, PR-035, PR-036, PR-037, PR-038, PR-039, PR-040, PR-041, PR-042, PR-043, PR-044, PR-045, PR-046, PR-047, PR-048 and PR-049. PR-021 and PR-044 received fresh focused verification in the current continuation after their resolution text was found to have been accidentally conflated, and PR-028 received its previously missing real-debugger and target-iOS qualification. The other recorded resolutions retain the evidence and limitations recorded in section 4; this count is not verification of the complete combined tree or a release-readiness claim. There are 0 open findings. An unchecked box does not necessarily mean that no code has been changed.

All 49 numbered findings now have recorded source-level resolutions with the evidence and limitations stated under their individual entries. PR-049's later post-PR-034 verification is recorded in its current resolution. The ten validation-gap checkboxes in section 6 are separate obligations and remain open until their matrix evidence is satisfied; closing the numbered findings is not a release-readiness claim.

**Tracking checker, verified for its stated scope:** `tests/port_review_tracking.py` reads only this report and checks finding IDs, resolution-checkbox coverage, summary counts, the recorded-resolution list and validation-matrix/checklist correspondence. `PYTHONPATH=tests python3 -m unittest -v test_port_review_tracking` freshly passed all 30 report-only tests on 2026-09-17 after the PR-021/PR-044 reconciliation, including the live report, neutral resolution wording, duplicate old/new summary rows and inline findings-heading references. The earlier blocked runs remain historical limitations and are not retroactively claimed as executed. This checker never changes checkboxes or certifies runtime fixes.

### Priority map

There are **49 numbered findings/risks**: nine P1 or conditional P1 entries, thirty-eight P2 entries and two P3 entries. Severity describes the potential impact of the stated failure path, not proof that it has occurred on the currently tested phone.

| Review focus | Entries | Why it matters |
| --- | --- | --- |
| Global stalls, restart and kernel publication | PR-001, PR-004, PR-005, PR-021, PR-024, PR-033, PR-044, PR-047 | Unbounded service waits; unrecoverable root transitions; unvalidated kernel publication; resuming non-executable code; falsely completing primitive handoff; injection failure leaving peers suspended; restart-helper output capacity exceeding its allocation |
| Signature and loader contracts | PR-007, PR-008, PR-009, PR-010, PR-025, PR-027, PR-034, PR-038, PR-040, PR-048, PR-049 | Mode-dependent normalization/architecture; discarded installation errors; binary UUID misuse; incomplete command outputs; omitted trustcache hashes; incomplete check-in capabilities; pathname reopening losing file identity; Mach failures escaping through a syscall return convention |
| Credentials and migrated binaries | PR-022, PR-023, PR-031, PR-032, PR-035, PR-041 | Reversed app classification; incompatible retained provider; incorrect donor identity/group construction; reusable attribute mutation; version-gated loss of the credential-drop barrier |
| RPC, hook and request lifetimes | PR-002, PR-012, PR-013, PR-014, PR-017, PR-020, PR-026, PR-028, PR-029, PR-030, PR-039, PR-043 | Caller identity, configuration/request scope, variadic ABI, diagnostic behavior, reply counts/ownership, endian parsing, external thread/trampoline state and fork readiness/acknowledgement |
| Bootstrap, packages, allocation and special routes | PR-003, PR-006, PR-011, PR-015, PR-016, PR-018, PR-019, PR-036, PR-037, PR-042, PR-045, PR-046 | Lost selection fallback; leaks; incorrect upgrade decisions; partial file publication; stale incremental binaries; pipe framing; allocation refusal reported as success; port-range wrap; incomplete XPF teardown; user interaction gating package services; replacement allocation refusal discarding the previous static trustcache |

The port-specific architectural risks are not synonymous with every inherited defect. Section 5 records adaptations that are already present and hypotheses that were rejected; section 6 lists the remaining work needed before implementation/runtime qualification. The strongest missing-adaptation findings concern shared trust semantics, execution architecture, client ABI migration, credential construction, completion/failure propagation and coherent builds. The ordinary source-level fixes in PR-003 and PR-022 are genuine port regressions, not hypothetical new-OS failures.

## 1. Scope, evidence and interpretation

This review compares four distinct inputs rather than treating a large two-way diff as a specification:

1. Upstream Dopamine 2, including the exact common ancestor used by the legacy fork.
2. Upstream Dopamine 3, including the exact upstream base of the current port.
3. The legacy Dopamine2-roothide fork, to recover the intent and dependencies of its patches.
4. The current Dopamine 3 roothide implementation, including pre-existing uncommitted changes.

The directory name is historical. The reviewed checkout is `/Volumes/data/.codex/worktrees/190a/Dopamine2-roothide`, on `port/roothide-dopamine3`. It was resumed in place; this review does not create another worktree. Independent reference clones live under ignored `.build/port-review/refs/`.

Every finding must distinguish: a current source defect; a probable compatibility risk requiring a specific validation; an inherited legacy defect; an already corrected defect; or a discarded hypothesis. A source-level failure path is not automatically evidence that a particular device panic followed that path. Conversely, a successful build or source-shape assertion does not prove runtime ABI compatibility.

The original inventory did not authorize device installation, activation, reboot, implementation fixes, commits or pushes. The subsequent user request authorizes fixing and verifying the findings in place; it does not change the historical scope of the inventory. Existing local Wolf identifiers, testing controls, diagnostic artifacts and uncommitted experiments are preserved.

## 2. Baselines

These are the verified review inputs. The reference-clone comparison below records their state at review start; findings about these snapshots do not imply that a subsequently changed upstream head has the same contents.

| Input | Commit | Role |
| --- | --- | --- |
| Current committed port | `21c153f114cb5f0171822203a75c764946e1958f` | Committed runtime/credential integration fixes, plus dirty working-tree changes reviewed separately |
| Upstream 3 base | `939a3a21400f0dc6d6163b2b5999ba2ace0d2732` | Dopamine 3.0.9 baseline inherited by the port |
| Locally fetched upstream 2 branch | `ff285a1643fc95fa47f3969e24337e6739fd614c` | Contemporary 2.x reference, not the legacy fork point |
| Legacy roothide 2 | `3824f2731275423c970ff6ed6685957dec269073` | 2.4.9.27 |
| Common upstream/legacy ancestor | `9cb54b37fda115c698c120c2c822c21463a60aec` | Required to separate original roothide changes from later upstream 2.x development |

Primary upstream repositories: [Dopamine 2](https://github.com/opa334/Dopamine/tree/2.x), [Dopamine 3](https://github.com/opa334/Dopamine/tree/3.x), [legacy roothide](https://github.com/roothide/Dopamine2-roothide/tree/v2.4.9.x).

Initial tracked modifications: `Application/Dopamine.xcodeproj/project.pbxproj`, `Application/Dopamine/UI/DOMainViewController.m`, `BaseBin/launchdhook/src/update.m`, `BaseBin/systemhook/src/main.c`, `BaseBin/systemhook/src/roothider_main.c`, `tests/run_host_tests.sh`. Initial untracked implementation includes `dyld_dispatch.c/.h` and loader/update tests; untracked private coordination and IDA artifacts also exist. The exact starting status and tracked patch are saved in `.build/port-review/evidence/initial-status.txt`, `initial-untracked.txt` and `initial-tracked.patch`.

Initial top-level diff sizes, excluding the dirty working tree: upstream 2 branch to upstream 3: 256 files; upstream 2 branch to legacy roothide: 212 files; upstream 3 to committed current port: 244 files. These are scope indicators, not completed-review counts.

Fresh independent clones of all three references completed successfully. Their HEADs exactly match the three reference SHAs above; there is no newly discovered branch-head drift. Full source diffs and a per-file comparison matrix are saved under `.build/port-review/evidence/`. The common-ancestor-to-legacy diff contains 610 files and includes considerable upstream development; it is not a clean roothide-only patch series. The 2.x-to-legacy and 3.x-to-current comparisons are used together to isolate port intent.

## 3. Architecture and adaptation matrix

The important unit of adaptation is a **lifecycle contract**, not an isolated hunk. Keeping a legacy roothide function unchanged can be correct for path branding and wrong for the signature, credential or executable-memory contract underneath it.

### 3.1 Upstream changes and corresponding port obligations

| Boundary | Dopamine 2 / legacy roothide assumption | Dopamine 3 change | Current port assessment |
| --- | --- | --- | --- |
| Exploit selection | Legacy exploit set plus roothide palera1n transport | Additional upstream exploit families and newer OS/device eligibility | Main upstream exploit implementations are retained; port deltas are palera1n and the ANE header naming correction. Stale preference fallback was lost: PR-003. Exploit algorithms were not independently re-proven. |
| Physical-memory access | Older handoff mapping and PPL-era assumptions | Changed userspace physical mapping, runtime host/ASID handling, SPTM/PAPT translation and TXM-aware memory policy | Current kernel/primitives/translation implementations retain upstream 3. Shared structure changes require rebuilding/migrating consumers (PR-023); allocator and dormant wrapper defects remain (PR-036/019). Do not import legacy primitive implementations wholesale. |
| Kernel metadata | Fork-specific XPF namecache/AMFI sets | Upstream 3 XPF and additional SPTM/TXM inputs | App-side DORoothidePatchfinder supplements upstream results. Dirty hot-update code preserves supplemental fields instead of requesting nonexistent fork sets. Monitor-resource teardown is incomplete (PR-042); exact firmware pattern/layout coverage remains a runtime/binary-validation obligation. |
| Root/bootstrap identity | Two branded hidden roots, cross-links, no stable /var/jb public root | Upstream app-identity support and newer bootstrap/launchctl requirements | Root model is retained; .AppIdentifier handles a custom bundle ID. Root transition recovery and final marker persistence are incomplete (PR-004); metadata is inconsistent (PR-011). |
| launchd interception | Legacy inline interception could consume bootstrap RPCs as a side effect | Symbol rebinding and explicit XPC/Mach dispatch | Explicit private bootstrap dispatch is now present and compiler-tested. Domain 5 remains roothide; upstream's new app domain is 6. Main lifecycle and spawn integration were traced, not merely symbol-compared. |
| Re-exec / boomerang | Primitive handoff plus suspended launchd resume | New primitive state, hookd lifecycle and control-port requirements | Fresh control-port resume and error checks are present, but primitive acquisition can fail while recovery reports success (PR-033), and service startup is unbounded (PR-001). A 2-to-3 userspace-only update is explicitly unsupported. |
| Executable-page mutation | Older writable/executable patch assumptions | hookd/external-memory operations and stricter executable-page policy, especially the newest OS family | Writer, client/server framing and external adapter source passes are complete. Failed RX restoration can still resume the target; reply ownership/counts and trampoline/thread state have defects (PR-024/026/029/030). Mocked operation ordering is not a CPU/cache/PAC proof. |
| dyld and library loading | Legacy gDyld/public-hook assumptions, recursive dependency pre-trust as an alternative to merged dyld | New dyld variants, private API/dispatch evolution and PAC-sensitive loader entry points | Both implementations, including dirty dispatch changes, were reviewed. The captured matcher passes six local test modes; caller-side failures are discarded (PR-025). Recursive trust and selected-slice behavior remain different (PR-007/008/009). |
| Signatures / TXM | Brand the first code page and publish the resulting CDHash | Signature descriptors from file/process/allocation, TXM flag normalization, server-side attachment | Ordinary path preserves the new APIs; recursive processing is not equivalent (PR-007). INFO outputs, batch insertion and failure reporting remain defective (PR-034/038/010). Cross-process reopening can lose file identity, and the normalization hook returns transport errors through the wrong result convention (PR-048/049). |
| Credentials / persona | Legacy direct credential editing and helper assumptions | Newer credential ownership/update model and restricted persona elevation from iOS 17.6 | Full copy/helper/check-in pass completed. Upstream mapped atomic reference operations are retained, but requested UID/group construction and attribute reuse are incorrect in defined cases (PR-031/032/035). Actual kernel reference/token behavior still requires target validation. |
| Child patching / exec | Suspend child, ask jailbreakd to patch it, resume according to caller flags | Must coexist with hookd, new credentials and newer launchd/dyld sequencing | Ordinary RPCs remain unbounded (PR-001). The bounded credential request does not solve the whole service graph. Configuration/enumeration, exception tracing and the independent remote dyld backend have separate problems (PR-012/020/021). |
| Namecache / sandbox | Hardcoded pre-16.4 versus modern namecache structures and raw list publication | Broader kernel families with newer translation/protection machinery | Source review covers both list implementations and extension issuance/consumption. Kernel publication lacks an established synchronization/recovery contract (PR-005/006). Check-in can complete without its required capabilities (PR-040). |
| App/preference hiding | Private CoreFoundation/CoreServices/SpringBoard hooks and per-client classification | Private signatures, selectors and service internals can change independently of exploit support | Audit-token API adaptation is present. Other hook contracts largely remain inherited; scoped request identity, variadic forwarding and new-OS ABI validation remain problems/gaps (PR-013/014). |
| Tweak and safe modes | Select present/fallback hook provider and retain infrastructure even when tweaks are disabled | New hook provider needs an external memory adapter | Provider-transition and daemon-lifecycle source passes are complete. Relative dependencies and prior export checks are positive. Infrastructure injection despite some safe-mode requests is intentional; missing provider-load results and ABI eligibility must not be mistaken for validated degraded-mode behavior. |
| Update / recovery | Replace basebin, retain loader vnode lifetime and refresh offsets | 2-to-3 primitive mapping incompatibility; new generated dyld/offset state | Reboot guard is retained, dirty offset overlay is correct in principle for the same kernel, but file publication errors can be hidden (PR-015). Full cold-start, re-jailbreak, userspace-reboot and hot-update paths must be tested separately. |
| Packages / build | Roothide-relative package layout and fork-specific helper binaries | Additional launchctl compatibility and modern dependency/toolchain requirements | launchctl retains required rewriting and installation ordering. Header dependency, package selection and binary migration gaps remain (PR-011/016/023); the privileged helper has a separate lexical version gate (PR-041). Standalone/Corellium is deliberately rejected rather than falsely advertised as ported. |

Primary evidence for this matrix is the pinned four-way source comparison, especially `DOJailbreaker.m`, `DOExploitManager.m`, `DOBootstrapper.m`, `libjailbreak/src/info.h`, `primitives.c`, `physrw*.c`, `translation.c`, `kernel.c`, `signatures.c`, `basebin_gen.m`, `launchdhook/src/main.m`, `spawn_hook.c`, `roothider.m`, `update.m`, the RPC domain tables, and the build/package manifests. Full diff inventories are retained in the evidence directory. A diff-inventory entry is not counted as a completed semantic review of that file.

### 3.2 Lifecycle model used in this review

**Cold activation:** app resolves upstream and roothide offsets -> acquires primitives/credentials -> selects or re-randomizes the two-root bootstrap -> rebuilds/branded-trusts basebin -> injects launchdhook -> prepares the generated loader and packages -> initiates the intended restart path. The initial injection has its own explicit completion and worker/suspension-lifetime requirements (PR-044); it is not covered merely by reviewing boomerang's later recovery. A failure after launchdhook injection is not equivalent to a pristine non-jailbroken app failure; rollback/reboot policy has to reflect already-live state.

**launchd re-exec:** old launchd preserves primitive state via boomerang and coordinates hookd -> new launchd restores primitives -> applies update-stage state where applicable -> publishes the branded systemhook alias -> starts/attaches jailbreakd -> ordinary children are suspended, patched and resumed -> their own check-in and library loads exercise the next RPC/trust boundary. Package-daemon registration is additionally delegated to the startup helper rather than loaded identically to upstream; watchdog notification must not be confused with completed service recovery (PR-045). “launchd resumed” establishes only the first part of that sequence.

**Ordinary launch versus late loading:** spawn-time recursive trust follows Mach-O dependencies using an inferred architecture/rpath context. Late dlopen-family operations need the actual caller/loader context and compatible trust/attachment behavior. These paths are not interchangeable merely because both eventually call a CDHash upload helper.

**Hot update:** stage new basebin -> validate/pre-trust it -> publish files -> restart with preserved primitive state -> refresh offsets -> publish new generated dyld while preserving old mapped vnode lifetime. The current source does not turn all those steps into an all-or-nothing transition. A full device reboot is a distinct boundary, required for 2-to-3 mapping changes, not a substitute for validating same-generation hot update.

### 3.3 Concrete lower-level changes that must travel together

**Mapping and translation are a coordinated ABI:** upstream 2 places the physical mapping at `(L1_BLOCK_SIZE * L1_BLOCK_COUNT) - 0x1000000000`; upstream 3 uses `- 0x3000000000` (`physrw.h:9`) and adjusts physical-to-user addresses for the level-1 address range (`physrw.c:24-26`). Its magic PTE window also moves from `L1_BLOCK_COUNT - 1` to `- 3` (`physrw_pte.h:9`). ASID handling now selects an eight-bit or sixteen-bit field and has an explicit fallback (`physrw_pte.c:16-62`), while translation/kernel helpers understand SPTM/PAPT and TXM state. The uploaded sources contain additional version-gated branches; their presence is not proof that every such firmware is supported by this port. Mixing an old mapped address convention with new restored state is not a valid migration.

**Consumer structures changed even where names did not:** relative to upstream 2, `kernel_primitives` inserts `krwMinSafeReadSize` before the call/allocator fields and adds `physaccess_mapped` (`primitives_external.h:18-33`). Legacy RootHide already backported the minimum-read-size member; `physaccess_mapped` remains an additional change when comparing that fork to the current port. `system_info` adds monitor constants, symbols and structures. A matching symbol name and unchanged consumer C source do not establish binary compatibility; PR-023's old-provider offset mismatch is a concrete example. Serialization of newly added RootHide fields is present, but already-installed clients must also be replaced or proven layout-independent.

**Stable action/domain numbers are not complete wire or native ABI compatibility:** `jbserver.h` retains the Mach magic/check-in action while inserting forceCSAdhoc before the reply's fixed strings. Its total aligned reply size remains 3,112 bytes in the Darwin LP64 layout, but the root offset moves from 49 to 50. The trust-file request grows from 88 to 96 bytes. Exported native check-in/trust functions also gain arguments without a symbol-name change. PR-023 records the compatibility obligation and source-layout fixture; preserving legacy domain 5 is correct but protects only that separate routing contract.

**Credentials now depend on valid mapped access:** the upstream-3 `kauth_cred_ref/unref/hold/drop` helpers use `kaccess_mapped` and atomic operations, including the separate writable credential reference representation (`kernel.c:232-300`). Copy/update callers must preserve those distinctions rather than reintroduce the legacy direct-field model. The donor also has to produce the requested identity, not merely respond before its timeout (PR-031/032), and caller-supplied spawn state must survive the workaround (PR-035).

**New transport helpers must cover the right callers:** the remote-task `mach_ports_lookup` workaround allocates its result in the calling task and chooses the newer kernel entry point by availability (`stock_fixes.c:12-59`). The reviewed remote-task consumers include its header; the raw remaining self-task lookups are not automatically omissions. The single-exchange Mach transport and explicit bootstrap dispatch adaptations are present. Their success-path correctness does not supply deadlines, reply cardinality checks, or successful primitive acquisition (PR-001/026/033).

**The two loader modes need one observable contract:** the merged loader can process file/process/allocated signatures and TXM normalization; the fallback path prepares dependencies and intercepts late loads through gDyld or gAPIs. The current arm64e gAPIs strategy avoids vtable writes and publishes a saved method before making its replacement reachable (`dyld_dispatch.c:79-91`). Its exact matcher and musttail calls are positive adaptations, not proof that all private ABIs or all firmware match. Both modes still converge on the flawed shared trustcache insertion, and both must propagate failed capabilities and hook installation rather than merely report check-in.

**The new signature descriptor is not an end-to-end file reference:** transmitting a caller-local descriptor number and reopening its pathname can select a replacement file rather than the object being loaded (PR-048). Likewise, a transport status must be translated at the syscall adapter boundary, not returned as a nonnegative fcntl result (PR-049). These are identity and completion contracts underneath both successful RPC delivery and apparently correct signature bytes; they should be checked before interpreting a later loader rejection as solely a missing hook.

**The root model is intentionally not upstream rootless:** a branded primary root, secondary writable root and synthetic systemhook alias replace a stable public /var/jb mount. Existing literal /var/jb strings must be evaluated in context: the libroot package is intentionally unpacked from that payload prefix (`DOBootstrapper.m:1272-1289`), and commented upstream fakelib paths are not active missing conversions. Correct adaptation is preserving RootHide's path semantics while retaining the new signature, credential and primitive contracts underneath them.

## 4. Current findings

Severity: P1 = boot/system-service failure or important global correctness defect; P2 = functional failure or robustness issue under a specific condition; P3 = lower-impact or currently unused footgun. Origin is stated separately: an inherited defect is not mislabeled as a new port regression.

### PR-001 - P1 - Ordinary child-patching RPC can still stall launchd indefinitely

- [x] **Resolution:** the forkfix handshake now closes each branch's unused pipe ends before either peer waits, requires the one-byte readiness and acknowledgement messages to complete, retries the raw read/write wrappers on their positive `EINTR` convention, and bounds acknowledgement waits with a five-second raw `poll` syscall. EOF, timeout, short/error I/O and repair refusal are terminal instead of being treated as a completed repair; the parent kills the child before aborting when readiness, repair or acknowledgement fails. The initial pipe setup also closes a successfully created first pipe if creation of the second pipe fails. `PYTHONPATH=tests python3 -m unittest -v test_fork_handshake` first reproduced the ignored EOF/error, missing EINTR retry and unused-end failures, then passed all eight actual-source scenarios after the repair (one unittest, ASan/UBSan, exit 0). `make -C BaseBin/forkfix forkfix.dylib` then rebuilt the arm64e dylib successfully (exit 0). The fixture replaces fork, pipe, RPC, signal and raw syscall operations with observations; it performs no real fork, process kill or kernel repair. This closes the defined handshake/failure-boundary defect, not the unclaimed question of concurrent libc fork calls.

**Status:** confirmed unbounded failure path; inherited roothide architecture, not a demonstrated explanation of the latest panic.

**Evidence:** `BaseBin/launchdhook/src/roothider.m:269-276` synchronously calls `jbdSpawnPatchChild` after suspending a child. `BaseBin/libjailbreak/src/roothider/jailbreakd.c:276-302,400-413` uses `xpc_pipe_routine` with no deadline. The spinlock and exec/trace requests use the same transport (`416-495`). `BaseBin/jailbreakd/src/server.m:91-113` only replies after patching and optionally resuming the child.

**Trigger/consequence:** a live but stalled daemon, blocked patch operation, or dependency cycle prevents the RPC from returning. The caller cannot reach its child-kill/error path, and a launchd spawn worker remains blocked. A dead service and a nonresponding live service are different cases; dead-port recovery does not bound the latter.

**Counter-evidence:** the new credential preparation request has an explicit deadline and a single-inflight limit (`jailbreakd.c:350-397`), so that previously fixed donor wait must not be reported as wholly unbounded. This protection does not cover the ordinary child-patching family.

**Required adaptation/validation:** define bounded failure and ownership of a suspended child for every launchd-to-jailbreakd operation; test a nonresponding service, interrupted request, daemon exit, and late reply without performing a live device failure injection during this review.

**Expanded lifecycle pass:** `hookd_provider.c:15-43` waits for a newly spawned hookd check-in without a timeout or child-exit observation, and the common Mach exchange also has no deadline (`libjailbreak/src/mach_msg_exchange.h:19-22`). `roothider/common.m:156-176` waits indefinitely for SSTOP. The separate exec-trace wrapper waits on `traced` and `detached` without a deadline (`systemhook/src/roothider_main.c:368-407`). The internal command spawner deliberately leaves a failed child stopped and returns 202 (`roothider/common.m:742-751`), unlike the ordinary launchd posthook which kills it. These paths need their own ownership/failure contracts; a single five-second donor timeout does not bound activation, daemon startup, exec or restart.

**Release recovery is deliberately different:** `roothider/jailbreakd.c:170-177` aborts in `reactiveJailbreakdPort` when ENABLE_LOGS is disabled, before its development-only restart logic. The comment explains that restarting loses private service state. Therefore a dead service is not silently recoverable in the default release configuration either. This is an explicit fail-stop policy to account for in recovery testing, not proof that automatic restart is always safe or should simply be enabled.

### PR-002 - P2 - Global dyld-mode setter checks launchd's identity instead of the caller's

- [x] **Resolution:** the existing repair uses the requesting PID plus its audit token, matching the platform-domain implementation, and rejects a failed status query or missing token. Root admission and the separate blacklist-domain gate remain intact. Fresh continuation verification: `python3 -m unittest discover -s tests -p test_loader_mode_identity.py -v` passed all seven actual-source caller-identity cases (one unittest, ASan/UBSan, exit 0), covering root, platform, ordinary, blacklisted, refused-status and reused-PID decisions. Process identity queries and mode state are fixture observations; no actual RPC, process query or loader-mode change occurs. The older blocked pre-fix result remains unavailable and was not retrieved through another route. This closes the source authorization defect, not target audit-token or loader-mode runtime qualification.

**Status:** confirmed source defect, inherited unchanged from legacy roothide.

**Evidence:** `BaseBin/launchdhook/src/jbserver/jbdomain_roothide.c:158-183` extracts the caller PID and UID, but `csops(getpid(), ...)` at line 164 reads the server's platform-binary flag. The authorization condition at line 166 consequently tests launchd's privileges. Domain admission only excludes blacklisted callers (`14-23`); action 9 exposes this setter (`266-273`).

**Consequence:** the intended root-or-platform-caller restriction does not hold for a reachable unblacklisted client. An unintended request can change the global loader mode, affecting subsequent spawn/trust behavior. This is a caller-identity correctness defect even in a trusted installation, not evidence that it caused a previous crash.

**Required adaptation/validation:** validate the requesting identity, not the host process; cover root, platform, ordinary unblacklisted and blacklisted callers. No request changing device state was sent.

### PR-003 - P2 - Upstream's stale exploit-preference fallback was removed

- [x] **Resolution:** all three selected-exploit getters now fall back to their supported preferred choice when a stored identifier is missing, unavailable or not a string; valid supported selections are retained. `python3 -m unittest discover -s tests -p test_selection_fallback.py -v` compiled the actual lookup/selection methods with Foundation and ASan/UBSan. The pre-fix run failed 9 of 15 cases; the post-fix run passed all 15 cases (one unittest, exit 0). The fixture uses inert objects and exercises kernel/PAC/PPL preferences independently; it loads or executes no exploit code. This closes the source-selection defect, not the separate device-support matrix.

**Status:** confirmed port regression relative to the pinned Dopamine 3 baseline.

**Evidence:** `Application/Dopamine/Jailbreak/DOExploitManager.m:112-136` only falls back to the preferred exploit when the preference is absent. `_findExploitWithIdentifier:andType:` searches supported exploits and returns nil for an unavailable stored identifier (`48-53,83-94`). Upstream 3 additionally checks membership in `availableExploitIdentifiersForType:` before honoring each of the three stored preferences; the port deletes both that helper and all three checks.

**Trigger/consequence:** a migrated, renamed or no-longer-supported kernel/PAC/PPL selection survives in preferences. The app chooses nil instead of the working supported default. This is not an assertion that unsupported exploit code is executed: the supported-set filter correctly prevents that, but activation can incorrectly report that a required component is unavailable.

**Required validation:** valid selection, missing preference, removed identifier, and identifier no longer supported after an OS/build transition, independently for all three types.

### PR-004 - P1 - Two-root bootstrap re-randomization has no recovery transaction

- [x] **Resolution:** re-randomization now writes an atomic journal containing the old/new brands before changing either root, makes each primary/secondary rename and cross-link publication idempotent, and clears the journal only after the paired layout is complete. `doBootstrap:` recovers an interrupted journal before enumerating/removing roots and repairs the uniquely recoverable legacy half-renamed pair left by the old implementation; a recovered pair is not immediately re-randomized again in the same attempt. The final `.installed_dopamine` commit marker write now returns a bootstrap-finalisation error on persistence failure instead of discarding it, and the requested removal branch runs immediately after privilege elevation rather than requiring `ensureJailbreakRootExists`/`prepareBootstrap` to succeed on a damaged pair. The earlier descriptor-relative `JBFixMobilePermissions` boundary repair remains intact. Fresh continuation verification ran `PYTHONPATH=tests python3 -m unittest -v test_bootstrap_transaction`: the ASan/UBSan host fixture injected failures after journal creation, both renames, both symlink publications and journal clearing, then recovered each temporary two-root layout to one consistent installed generation; it also repaired the legacy mismatched-name pair (one unittest, exit 0). The fixture mutates only temporary host directories and symlinks and performs no live bootstrap, privilege, ownership, reboot or device operation. This closes the source-level transition/recovery, marker-persistence and removal-ordering defects; interrupted live-device recovery remains part of V-07 rather than this checkbox.

**Status:** confirmed inherited failure path, particularly important while iterating activation and migration.

**Evidence:** `Application/Dopamine/Jailbreak/DOBootstrapper.m:932-965` first renames the primary root, then the secondary root, then removes/recreates the two cross-links, and refreshes the cached path only at the end. Each failed `ASSERT` reports an error and returns; no rollback or interrupted-operation recovery exists. `doBootstrap:` accepts an installation solely by `.installed_dopamine` (`987-989`) and attempts another re-randomization (`1024-1029`).

**Trigger/consequence:** if the primary rename succeeds but the secondary rename fails, the next attempt derives the new primary brand and tries to rename a secondary directory that still has the old brand. The installed environment remains inconsistent across retries. Failure between link removal and recreation produces another inconsistent state. These are concrete ordering defects, not a claim that ordinary successful renames fail routinely.

**Related marker hazard:** the final installation-marker write at `1292` discards errors, yet a missing marker causes `doBootstrap:` to delete the supposedly unfinished primary/secondary roots at `993-1000`. An installation that reached package configuration but could not persist its final marker can therefore be silently removed on the next attempt.

The requested removal flow also calls prepareBootstrap before reaching the removeJailbreak branch (`DOJailbreaker.m:693-726`). Therefore a root mismatch that prevents preparation can prevent the ordinary removal action from reaching its cleanup code. Recovery must not require successfully completing the same damaged transition first.

**Required adaptation/validation:** recoverable two-root state transitions and checked commit-marker persistence; simulate failures after each rename/link/marker step in a temporary host fixture, not against a live bootstrap. Merely retrying activation is not a recovery policy.

**Root-boundary validation also matters during permission repair:** `libjailbreak/src/util.m:75-86` disables upstream's guards for both var and var/mobile, because RootHide intentionally uses a secondary-root var symlink. The subsequent helper follows symlink targets with stat/chown (`49-69`). Consequently a different var/mobile or enumerated-file link can redirect ownership changes outside the intended bootstrap. The valid primary-to-secondary link must be allowed explicitly; blindly restoring upstream's complete symlink rejection would disable necessary RootHide repair. This is a source-level filesystem boundary, not an observed external ownership change.

**Permission-boundary implementation evidence - 2026-09-15:** the original normal-layout control passed, while real outside-link and pre-open replacement cases produced observed ownership requests for unintended objects. The passing 37-case suite additionally covers failures at primary/container/secondary/var/mobile acquisition, metadata and link checks, child lookup/open/stat, enumeration, ownership refusal, wrong-type mobile entries, modeled device changes, missing roots, already-correct owners, an already-open file renamed before mutation, and 40 repeated operations. No host ownership syscall is executed. The [POSIX open/openat contract](https://pubs.opengroup.org/onlinepubs/9799919799/functions/open.html) supports descriptor-relative lookup and final-component `O_NOFOLLOW`; subsequent mutation uses `fchown` on that descriptor. This remains a best-effort repair with the existing void public interface: skipped/unreadable entries and refused ownership changes are not reported as repaired. The tests do not qualify a live iOS bootstrap or provide a transaction against arbitrary concurrent directory relocation, mount changes or new hardlinks after the link-count check.

### PR-005 - P1 - Namecache publication lacks a validated synchronization and failure boundary

- [x] **Resolution:** both namecache publishers now validate the live topology after acquiring the real namecache writer lock, retain the participating vnodes through the operation with the kernel's vnode reference primitives, route every pointer/list mutation through a checked transaction, verify each write, and roll the transaction back in reverse order on any later refusal. The modern publisher invalidates the entry's `nc_counter` before linked-list mutation and republishes a new valid sequence only after the complete transaction verifies; rollback likewise occurs while invalid and republishes only after the old topology has been restored. A failed final open/path check therefore no longer leaves a partially published cache entry. Patchfinding now resolves the writer lock, exclusive-lock/unlock functions and vnode retain/release functions uniquely instead of assuming raw usecount edits are sufficient. Fresh verification on 2026-09-17: `PYTHONPATH=tests python3 -m unittest -v test_namecache_publication test_namecache_resources` passed three unittests, including 72 actual-source scenarios across both publishers and log modes covering repeated publication, writer-lock refusal, stale topology detected under the lock, vnode-retain refusal, write refusal, modern counter-publication refusal and rollback/topology restoration. The current exact iPhone12,1 iOS 26.0/23A341 release and research kernelcaches also passed `roothide_add_kernel_offsets`, uniquely resolving nonzero `nchashtbl`, `nchashmask`, `namecache_rw_lock`, `lck_rw_lock_exclusive`, `lck_rw_done`, `vnode_ref_ext` and `vnode_rele_ext` addresses in both images. This closes the source-level unsynchronized/partial-publication defect. Exact structure-field/PAC representation qualification across every supported kernel family and real competing cache activity remain V-05 target obligations; the iOS 26 evidence is not generalized to untested firmware.

**Status:** confirmed implementation hazards; broader-firmware compatibility risk. No particular kernel panic is attributed to this path.

**Evidence:** `BaseBin/libjailbreak/src/roothider/unsandbox.m:90-96` selects only pre-16.4 and 16.4-or-newer implementations. Both copy locally declared kernel structures (`unsandbox.h:5-125`; `unsandbox2.m:34-48`), perform non-atomic read/modify/write reference increments, then mutate several linked lists from cached snapshots. In the modern path (`unsandbox2.m:142-173,220-312`) kernel-read/write results and namecache-node validity are not checked, no namecache synchronization is acquired, and no `nc_counter` transition accompanies the mutations. A failed final open/path lookup (`325-336`) returns failure after those mutations without restoring state. `launchdhook/src/roothider.m:140` converts that failure into a launchd assertion.

**Architectural concern:** Apple XNU's [namecache implementation](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/vfs/vfs_cache.c) uses writer locking and a valid/sequence counter for lockless readers; its [vnode declarations](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/sys/vnode_internal.h) specify locking for the corresponding lists and references. The port supplies neither those mechanisms nor a demonstrated equivalent quiescent interval. A newer primitive that permits writes does not make a multi-list update atomic.

**Counter-evidence:** the publicly inspected [current namecache declaration](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/sys/namei.h) still has the broad field order of the copied modern structure. Therefore this review does **not** assert that all newer firmware necessarily has wrong offsets. The inspected upstream `info.c:117-124` also agrees on `fileproc.glob = 0x10` and `fileglob.data = 0x38`. Exact target-build layout, pointer representation, object identity, lifetime and synchronization remain separate obligations; finding the two globals alone does not validate them.

**Required adaptation/validation:** validate every used layout and list invariant for each supported kernel family before mutation; define the publication ordering and failure policy; verify repeat publication/re-exec and competing cache activity. Source-shape tests that merely find `nchashtbl` in an offset dictionary do not cover this contract.

### PR-006 - P3 - Namecache tail-file lifetime and diagnostic work are not bounded to the operation

- [x] **Resolution:** both implementations now keep the temporary path and descriptor in the operation's cleanup scope, use exclusive temporary-file creation, retain the tail through the complete operation, and unlink/close it on every returning path, including descriptor zero and failures after creation. Cleanup refusal is diagnosed without relabeling an already-published operation as a publication failure. Unconditional printing and diagnostic-only tail reads are removed. Hash/child diagnostics and their call-site reads compile out with logging disabled; enabled traversals stop after at most 128 entries or a failed structure read, and names remain terminated. `python3 -m unittest discover -s tests -p test_namecache_resources.py -v` reproduced 42 failing scenarios of 52, then passed all 52 after repair (one unittest, ASan/UBSan, exit 0). The complete actual implementations and actual structure declarations execute against operation-owned descriptor/file and memory observations, covering 16 repeated operations, every existing returning failure boundary, descriptor zero and cyclic child/hash diagnostics in both versions and logging configurations. No real kernel/cache mutation or device operation occurs. This closes the resource/diagnostic lifetime defect, not PR-005's independent kernel-layout/publication qualification or an OS refusal to remove a temporary file.

**Status:** confirmed inherited resource/diagnostic footgun.

**Evidence:** `unsandbox1.m:52-71` and `unsandbox2.m:90-109` create a random `/tmp` file and keep its descriptor open on success and subsequent failures. No descriptor is returned to the caller for cleanup, and the file is never unlinked. Keeping a tail entry alive during mutation is intentional; losing the handle permanently is a separate lifetime issue. The final cleanup only closes the three other descriptors.

Unconditional `printf` calls remain in both helpers. The `print_nc`/`print_nc2` walks and child-list traversals are invoked even when their `JBLogDebug` output is compiled out (`unsandbox1.m:138-154,175-176,259-260`; `unsandbox2.m:176-192,213-214,314-315`). Those walks have no cycle/iteration bound and continue reading kernel memory in otherwise log-disabled builds.

**Required validation:** repeated operations should not accumulate descriptors/files; release diagnostic configuration should eliminate diagnostic traversal, not just formatting output. No claim is made that a single extra descriptor explains an activation failure.

### PR-007 - P1 on SPTM/TXM - Recursive trust bypasses Dopamine 3's signature normalization and attachment path

- [x] **Resolution:** recursive trust now passes each ad-hoc slice through the same `trust_signatures` contract used by the ordinary file path before it continues dependency traversal. The recursive path materializes the opened slice as an owned allocation-backed `siginfo`, propagates normalization/trust/attachment failures, frees any replacement owned bytes returned by the shared contract, and rechecks pathname identity before continuing. This preserves the existing recursive CDHash collection while adding Dopamine 3's SPTM/TXM normalization and detached-attachment behavior instead of duplicating that policy. Fresh verification on 2026-09-17: `PYTHONPATH=tests python3 -m unittest -v test_recursive_trust_failures test_recursive_trust_arch_transport` passed both focused unittests, including mandatory/weak dependency outcomes, branding and normalization refusal, pathname replacement and mixed arm64/arm64e dependency selection. The actual shared `trust_signatures` fixture was also rebuilt and run against bundled killall, dash and pam_unix.so; all three passed the on-disk branding, repeated-trust and TXM source/normalization/attachment matrix, including allocation/file/process-backed inputs and the four CS_ADHOC/TeamID combinations. This closes the source-level trust-contract divergence. V-06 remains open for target loader/runtime equivalence, including real sandbox/attachment acceptance and both global dyld modes on-device.

**Status:** confirmed architectural omission in the recursive service; device-level impact depends on the encountered signature and whether another loader path repairs it later.

**Evidence:** `launchdhook/src/roothider.m:220-224` selects recursive trust when `dyld_patch_enabled()` is false; the app initializes this setting with fallback NO (`DOJailbreaker.m:654`). `libjailbreak/src/roothider/signatures.m:244-290` computes/brands CDHashes and recurses, but never invokes `trust_signatures` or signature attachment. The roothide RPC wrapper (`jbdomain_roothide.c:32-59`) likewise only collects and uploads CDHashes. In contrast, Dopamine 3 introduced `libjailbreak/src/signatures.c:337-385` to normalize the CS_ADHOC/TeamID combination on SPTM systems and attach the modified signature. `jbdomain_systemwide.c:157-170` also supports explicit server-side attachment.

**Consequence:** the two modes do not implement equivalent trust contracts. A signature needing the new TXM normalization can be accepted by the recursive RPC without receiving that normalization or detached attachment. Bootstrap/package dependencies are particularly important because the default mode takes the recursive branch before their execution. This finding does not assert that an ordinary correctly formed ad-hoc signature requires detached attachment, or that the earlier PAM failure was caused by this specific omission.

**Correction to initial hypothesis:** both upstream and current `trust_signatures` still restrict their CDHash modifications to ad-hoc signatures. Therefore “the recursive path forgot to trust all non-ad-hoc binaries” is **not** the established regression. The verified difference is normalization and attachment for signatures requiring it; non-ad-hoc traversal is a separate inherited behavior.

**Required adaptation/validation:** one signature-processing contract shared by single-file and recursive services; test both global dyld modes with the four CS_ADHOC/TeamID combinations, disk/allocated/remote signature sources, and a sandboxed loader requiring server-side attachment.

### PR-008 - P2 - Recursive dependency trust does not preserve the selected execution architecture

- [x] **Resolution:** recursive trust now carries an explicit execution-architecture selection through the entire dependency walk and `@rpath` resolution instead of reselecting every loader with the hardware-preferred slice. The selected main-executable slice updates that execution architecture to the actual slice used, including arm64e ABI fallback rules: a generic arm64e executable preference selects the current arm64e ABI when available, old-arm64e remains usable for libraries but not executables, and arm64/V8 fallbacks stay architecture-consistent. Runtime `dlopen` trust now includes the caller image's actual Mach-O architecture in the RootHide RPC (falling back to the main executable image), so dependency/rpath selection follows the running slice rather than the server host preference. The strengthened actual-source fixture builds a fat arm64/arm64e executable plus a fat dependency whose slices use different rpaths, deliberately converts the arm64e dependency to the old library ABI, and verifies both the selected main/dependency slice offsets and which architecture-specific leaf was traversed for explicit arm64, explicit arm64e-v2 and generic arm64e preferences. A separate RPC fixture verifies that runtime library trust transmits the caller's running architecture. Fresh `PYTHONPATH=tests python3 -m unittest -v test_recursive_trust_failures test_recursive_trust_arch_transport test_rpc_argument_contracts` passed all three unittests (including 36 RPC scenarios; exit 0), and fresh `make -C BaseBin/libjailbreak -j4 && make -C BaseBin/systemhook -j4 && make -C BaseBin/launchdhook -j4` rebuilt all arm64/arm64e consumers successfully (exit 0), with only pre-existing macro and pointer-qualifier warnings. This closes the source-level architecture/rpath mismatch; target runtime matrix coverage remains part of V-06.

**Status:** confirmed inconsistent selection logic; a mixed-slice executable/dependency fixture is needed to establish each affected runtime combination.

**Evidence:** `roothider/signatures.m:225-242` honors the spawn-preferred architecture only for the main executable. Every dependency falls back to the hardware-preferred slice, and `resolveRpaths:70-86` independently parses the hardware-preferred slice of each loader to obtain its LC_RPATH entries. The recursion cache is keyed by canonical path, not architecture (`205-214`). The ordinary Dopamine 3 collection path enumerates all mappable slices (`libjailbreak/src/signatures.c:114-133,180-194`).

**Consequence:** forcing an arm64 executable on arm64e hardware, or using a fat library whose slices have different dependency/rpath metadata, can cause the pre-trust walk to inspect and brand a different slice than the process loads. Trusting the host-preferred CDHash is not proof that the requested slice was trusted.

**Required validation:** arm64 and arm64e executables on arm64e hardware, explicit spawn architecture preferences, old/new arm64e library ABI, and fat dependencies with intentionally different rpaths/load commands. The test must inspect selected slice offsets rather than merely check that some hash was collected.

### PR-009 - P2 - The two slice-selection helpers disagree about old arm64e libraries

- [x] **Resolution:** corrected the reversed executable/library predicate in `macho_is_mappable`, matching the existing RootHide selector's old-ABI exclusion for executables. Added `tests/test_mappable_slices.py` with 16 admission cases across modeled arm64/arm64e hosts and real Mach-O constants. The first fixture compilation exposed the existing signed/unsigned comparison warning under the fixture's `-Werror`; that warning was explicitly allowed in the fixture. Fresh verification in the current continuation ran `PYTHONPATH=tests python3 -m unittest -v test_mappable_slices` against the current actual function and passed for both modeled host modes (one unittest, exit 0). The requested Dopamine 3 reference still contains the reversed `header->filetype == MH_EXECUTE` predicate, while the current port contains the repaired `!= MH_EXECUTE` predicate. This closes the source-level admission mismatch; it does not by itself certify V-06's mixed-slice runtime/rpath behavior.

**Status:** confirmed inherited upstream defect, retained in the port; it is not newly introduced by roothide.

**Evidence:** `libjailbreak/src/signatures.c:42` assigns `isLibrary = (header->filetype == MH_EXECUTE)`. Line 51 then admits the old arm64e ABI only when this variable is true. The accompanying comment says the opposite: old ABI libraries are mappable, executables are not. The roothide helper correctly rejects the old arm64e slice when its file type is MH_EXECUTE (`roothider/signatures.m:27-31`). The reversed predicate also exists in both pinned upstream versions.

**Consequence:** the ordinary all-slice collector excludes an old-ABI arm64e library while including an old-ABI executable slice that the roothide preferred-slice collector intentionally rejects. This can produce mode-dependent behavior even before considering the new loader hooks.

### PR-010 - P2 - Trust operations can report success after collection or trustcache failure

- [x] **Resolution:** recursive collection now returns a real status instead of silently dropping failures, distinguishes missing weak dependencies from required ones, stops the dependency walk on the first mandatory failure, checks CDHash-array growth, and propagates branding errors through the RootHide RPC. The RPC now uploads hashes only after successful collection, returns the trustcache publication result and always frees its collection buffer. Ordinary `trust_signatures` likewise returns a failed `jb_trustcache_add_cdhashes` result before attempting detached attachment. Recursive preparation now parses the object held by its opened descriptor and verifies that the canonical pathname still names that same device/inode/generation before and after pathname-based branding, returning `ESTALE` on replacement. TDD first reproduced false success for both collector and trustcache refusal in the actual RPC wrapper, and the real ordinary trust fixture reproduced ignored trustcache refusal. Fresh verification passes `PYTHONPATH=tests python3 -m unittest -v test_rpc_argument_contracts test_recursive_trust_failures` (36 RPC scenarios plus required-missing, weak-missing, branding-refusal, already-trusted and pathname-replacement recursive cases), and the actual `trust_signatures` host fixture passes for bundled killall, dash and pam_unix.so including the new upload-refusal case. Fresh `make -C BaseBin/libjailbreak -j4 && make -C BaseBin/launchdhook -j4` rebuilt the arm64/arm64e consumers successfully (exit 0). This closes the source-level false-success/error-identity defect; recursive TXM normalization/attachment and selected-architecture equivalence remain separately open as PR-007/008 and V-06.

**Status:** confirmed error-propagation defect; inherited in part, expanded by the new branding failure modes.

**Evidence:** recursive collection is `void`, with parse, resolve, open and branding failures returning only from the current recursion (`roothider/signatures.m:162-177,218-240,261-266`). Its RPC wrapper calls `jb_trustcache_add_cdhashes` without checking the result and returns 0 (`jbdomain_roothide.c:54-59`). The ordinary `trust_signatures` also ignores the upload result (`libjailbreak/src/signatures.c:393-395`).

**Consequence:** a permission/I/O/unsupported-signature or trustcache-allocation failure can be reported as successful preparation; the subsequent loader/spawn failure is then diagnosed at the wrong boundary. Missing weak dependencies need an explicit allowed outcome, but they must not make mandatory dependency or upload failures indistinguishable from success.

**Required validation:** missing weak versus required library, branding refusal, file replacement during preparation, allocation/upload failure, and successful already-trusted inputs. Preserve a failure path and diagnostic identity from the first failed dependency through the caller.

### PR-011 - P2 - Bundled package upgrade metadata is stale and comparison is not Debian-version aware

- [x] **Resolution:** the existing repair synchronizes the advertised libkrw version with the 2.0.4 control file and uses a separate Debian epoch/upstream/revision comparator, preserving the app comparator. Status parsing matches the complete package field, handles field order and CRLF stanzas, and requires an error-free installed state and a nonempty version. The historical pre-fix run reproduced 19 of 32 behavioral failures plus the metadata mismatch; its subsequently blocked result was not recovered. Fresh verification on 2026-09-15: `python3 -m unittest discover -s tests -p test_package_selection.py -v` passed both unittests, covering the actual package-selection methods and bundled/control metadata (Foundation, ASan/UBSan with fatal diagnostics, exit 0). Independently, `python3 -m unittest discover -s tests -p test_app_versions.py -v` passed the existing ten app-version checks (one unittest, same sanitizer configuration, exit 0). No package was installed and no device operation occurred. This closes package selection and version semantics, not the separate provider ABI migration in PR-023.

**Status:** confirmed source inconsistency; the libkrw version mismatch is inherited from the legacy input, while the numeric-component comparison is current port code.

**Evidence:** `DOBootstrapper.m:21-31` advertises libkrw 2.0.3, but `Packages/libkrw-provider/control:6` ships 2.0.4. `shouldInstallPackage:` (`528-537`) compares against the advertised version, so an installed 2.0.3 provider is considered current. `installedVersionForPackageWithIdentifier:` uses a prefix match for `Package: <identifier>` rather than an exact field value (`494-511`). `NSString+Version.m:19-33` discards all non-digit separators, including Debian epoch/revision syntax, suffix letters and `~` prerelease ordering; it is used for bundled Debian-package decisions as well as app version comparisons. PR-023 establishes a separate source-level ABI reason why retaining a legacy provider is consequential.

**Consequence:** upgrades can be skipped or misordered, and similarly prefixed package names can be mistaken for the intended package. This is not evidence that the 2.0.3/2.0.4 provider binaries have different kernel APIs; the verified failure is the selection contract.

**Host reproduction completed:** a temporary executable linked against the actual `NSString+Version.m` orders `1.0~rc1` above `1.0`, treats `1.0a` and `1.0b` as equal, and orders `1:1.1.1-2+dp3.1~rc1` above the corresponding release. The installed `dpkg --compare-versions` orders the left version below the right in all three cases. Results are in `evidence/validation/package-version-observations.tsv` and `debian-version-oracle.log` beneath `.build/port-review/`. This does not invalidate the separately passing app-version tests; the two version domains need different semantics.

**Required validation:** exact package-name matching, installed-state checks, synchronized package/control metadata, and Debian epoch, revision, letters and prerelease ordering separately from the app's three/four-component version format.

### PR-012 - P2 - Shared spawn configuration is neither atomically published nor type-safe

- [x] **Resolution:** the existing repair replaced the shared mutable cache with per-thread immutable snapshots and thread-exit cleanup; returned values are retained and the caller releases them. Reload detection compares device/inode/size/mtime/ctime, including equal or older timestamps; failed reloads keep the last valid snapshot and remain retryable, while removal clears it. The plist reader closes its descriptor on every path, performs complete EINTR-aware reads into owned memory rather than a truncation-sensitive mapping, and rejects changed snapshots and non-dictionary configuration. Blacklist elements are type-checked. Enumeration uses per-invocation `strtok_r` state and checks NULL/allocation failure. The historical pre-fix run reproduced 13 failures in `tests/test_spawn_configuration.py`; its earlier blocked post-fix command was not recovered. Fresh continuation verification: `python3 -m unittest discover -s tests -p test_spawn_configuration.py -v` passed all 20 actual-source scenarios, then passed again after enabling fatal sanitizer diagnostics (one unittest, ASan/UBSan, exit 0). Coverage uses ordinary temporary files, reference-counted XPC observation objects and eight real host reader threads, covering invalid/wrong-type data, replacement/reload, retained values, thread-exit ownership, read interruption/truncation and nested enumeration. No process is spawned or instrumented. This closes the configuration/ownership and enumeration defects within that source-level scope; it is not a ThreadSanitizer result, real-XPC parser qualification or target spawn certification.

**Status:** confirmed inherited failure paths; concurrency impact requires a multithreaded fixture.

**Evidence:** `systemhook/src/common/common.c:102-119` shares a mutable static XPC dictionary and timestamp without synchronization. It publishes the new timestamp before parsing, releases the old dictionary before a replacement succeeds, and calls a dictionary accessor even when parsing/stat races leave the dictionary NULL. `spawn_config_for_executable:140-145` checks that ProcessBlacklist is an array but not that each element is a string before `strcmp`. `xpc_object_from_plist:90-98` leaks its descriptor when mmap fails.

**Consequence:** an empty, partially written, invalid or wrong-type configuration can crash a spawn caller, including launchd; concurrent reload/read can use a released dictionary. A failed parse also remains cached until mtime advances again. Separately, `string_enumerate_components:55-65` uses stateful `strtok` around a callback; nested enumeration can corrupt the outer DYLD_INSERT_LIBRARIES walk.

**Host reproduction completed:** a temporary fixture containing the exact current enumeration function visits only one of two outer components when the first callback starts a two-component inner enumeration. The inner enumeration visits both components. This establishes the function's reentrancy defect, not that an existing production callback necessarily nests today. The untested concurrent configuration-reload risk remains a separate claim. Output: `.build/port-review/evidence/validation/nested-enumeration.log`.

**Required validation:** invalid/empty/non-dictionary plist, non-string blacklist entry, atomic file replacement, same-timestamp replacement, concurrent readers, and nested environment enumeration. A cached immutable configuration snapshot and explicit parse-failure behavior should be specified before implementation.

### PR-013 - P2 - Auxiliary hooks retain request state beyond the request and assume unverified private ABIs

- [x] **Resolution:** request classification is now scoped to the call that established it. `cfprefsd` saves/restores the prior client PID with `RHPidScope` and uses `@try/@finally` around the private daemon handler so exceptions cannot leak another request's identity. SpringBoard and LaunchServices object tags use reference-counted associated scopes that are removed/restored after the nested call instead of leaving permanent `NSString`/`NSURL` state. Every `_LSDReadClient` wrapper saves the outer `g_utrHide`, explicitly assigns the nested client's classification and restores the prior value in `@finally`; the iOS 26 `mimic:` resource selector is included. Returned LaunchServices collections are copied before filtering and correlated private arrays are type/count checked rather than mutated under unchecked index assumptions. Fresh verification on 2026-09-17: `PYTHONPATH=tests python3 -m unittest -v test_auxiliary_request_scopes test_lsd_query_filter_safety` passed both focused actual-source suites, covering nested PID scopes, reused object tags, nested UTType visibility and copy/index safety. A clean RootHide-Theos build of `BaseBin/roothidehooks` then compiled, linked, merged and signed both arm64 and arm64e variants successfully. This closes the source-level state-lifetime/mutability defects; per-OS private selector/ivar/C-ABI qualification and deferred-work behavior remain explicitly tracked under V-09 rather than being inferred from a successful dylib build.

**Status:** confirmed lifetime footguns plus explicit compatibility gaps; inherited code, not proven failures on every newer OS.

**Evidence:** `roothidehooks/cfprefsd.x:120-122` assigns the thread-local current client PID and returns through the original handler without restoring the previous value. Redirection consults that PID at `74-77`. Background/reentrant preference work on the same thread can therefore inherit another request's identity. `springboard.x:104-109` attaches a deny tag to a caller-provided identifier without clearing/restoring it; `lsd.x:98-107` similarly tags the NSURL. Reuse or nested handling of these objects is not isolated by a scoped request context.

The cfprefsd hook chooses two different C signatures solely at iOS 17 (`cfprefsd.x:97-105,142-150`); lsd uses many private selectors, KVC ivars and C symbols. Missing C symbols are silently skipped, while several lsd paths assert that private side effects made an operation fail (`lsd.x:108-110,150-152`). The port changes audit-token acquisition and linkage, but provides no per-new-OS contract verification for this hook surface.

**Required validation:** nested and deferred requests from differently classified clients; reused NSString/NSURL instances; preference daemon message ABI and selector/ivar existence on each supported OS family; explicit degraded behavior when a hiding hook cannot be installed. Do not equate a successful dylib load with functioning app/preference hiding.

The additional UTType path has a related but different scope problem: `_LSDReadClient` wrappers set the thread-local `g_utrHide` to YES, call the original method and unconditionally reset it to NO (`lsd.x:397-477`). They do not save/restore an outer value, reset the value when entering an unblacklisted nested request, or propagate context into deferred work. The `g_utrBusy` counter correctly guards the helper's own nested database queries (`283-325`), but does not fix nested/deferred client identity. Query filtering also mutates returned arrays and private `_pluginUnits` via KVC (`514-581`), relying on mutability and index correspondence that need checking on newer LaunchServices implementations.

### PR-014 - P2 - SpringBoard's fcntl wrapper consumes ten variadic arguments unconditionally

- [x] **Resolution:** both variadic interception sites now consume only the arguments their contracts require. The SpringBoard fcntl hook forwards known no-argument commands without touching a `va_list` and consumes exactly one machine-word argument for the remaining Darwin fcntl commands; its nested F_GETPATH lookup therefore also forwards one pointer rather than reading ten nonexistent values. The launchd sandbox hook consumes no variadic argument for `SANDBOX_FILTER_NONE` and exactly one filter argument for the other filter forms, preserving the existing RootHide mach-lookup decision. TDD verification first reproduced the original undefined forwarding with `PYTHONPATH=tests python3 -m unittest -v test_variadic_hooks` (fixture exit 1), then passed the focused actual-source fixture after repair (one unittest, ASan/UBSan, exit 0), covering no-argument F_GETFD/sandbox calls plus integer and pointer fcntl/filter arguments. A direct `make -C BaseBin/roothidehooks -j4` invocation could not run standalone because the Theos environment variables were not populated (`/makefiles/common.mk` and `/tweak.mk` unresolved); no build success is claimed from that command. This closes the C varargs over-read/forwarding defect; V-09 still requires private hook ABI validation on supported OS families.

**Status:** confirmed inherited C-level undefined behavior.

**Evidence:** `roothidehooks/springboard.x:17-30` calls `va_arg(..., void *)` ten times for every command, then forwards all ten values. Calls such as F_GETFD provide no optional argument; other commands provide one integer or pointer, not ten pointers. The wrapper itself calls `fcntl(..., F_GETPATH, filePath)` at line 9, which also enters this forwarding logic with only one optional argument.

**Consequence:** the hook reads beyond supplied arguments and assumes a variadic ABI/type layout rather than forwarding the actual command contract. A crash is not guaranteed on a particular ARM64 stack layout, but the implementation is not defined or portable. Validate no-argument, integer and pointer commands separately, including the wrapper's nested F_GETPATH call.

The same unconditional ten-argument extraction also exists in `BaseBin/launchdhook/src/ipc_hook.c:11-23`. That second site is `sandbox_check_by_audit_token_hook`, not another fcntl wrapper; its filter-dependent argument contract must be considered separately. The later repair must cover both interception sites; changing only the SpringBoard wrapper would leave the same C-level contract violation inside launchd. Neither wrapper was changed in the permission-boundary continuation.

### PR-015 - P2 - Generated dyld publication can report success without publishing the new file

- [x] **Resolution:** generated-dyld publication now checks every filesystem transition that determines whether the new linker is actually published: inflight copy, removal of the prior backup, current-to-old rename and inflight-to-current rename. `apply_dyld_patch` also requires the target symbol and LC_UUID and propagates failed writes. The enclosing basebin updater now rejects a failed source-directory enumeration, destination removal or item copy instead of continuing to a readable version file and reporting success. `python3 -m unittest discover -s tests -p test_basebin_publication.py -v` passed the hot-update copy-refusal, missing-current and successful-publication cases; `python3 -m unittest discover -s tests -p test_dyld_patch_publication.py -v` passed its symbol/UUID/write-failure fixture; and `python3 -m unittest discover -s tests -p test_update_publication.py -v` first reproduced both remaining false-success cases, then passed after the updater repair (three separate unittests, exit 0). The fixtures use temporary host files and stub the patch/merge/trust operations; no installed basebin is modified. This closes the false-success publication defect. It does not claim whole-update rollback after an earlier item was already replaced, nor qualify vnode lifetime on-device.

**Status:** confirmed inherited failure path, consequential during hot updates.

**Evidence:** `libjailbreak/src/basebin_gen.m:168-182` ignores errors removing an older backup, renaming the current dyld to `dyld.old`, and moving the inflight image into place; it logs “linker published” and returns 0 regardless. Earlier copy/directory/symlink operations also discard their outcomes (`132-157`). `apply_dyld_patch:58-60` returns before freeing an opened writable Mach-O when its symbol is missing, and its writes/LC_UUID presence are not all checked.

**Consequence:** an update can continue after failing to replace its loader, or after moving the previous loader away but failing the final move. This is a distinct boundary from the bootstrap root transaction in PR-004. The intended old-vnode lifetime preservation is correct; unchecked publication does not establish that lifetime transition succeeded.

The enclosing basebin updater has the same failure pattern: `launchdhook/src/update.m:53-91` removes/copies each archive item while discarding filesystem errors, then treats a readable new `.version` as sufficient to set the update handoff variables. A partially replaced basebin can therefore look like a successful update. Trustcache preparation is checked, but that does not validate subsequent file publication.

### PR-016 - P2 - Incremental build dependencies can package binaries compiled against stale headers

- [x] **Resolution:** the existing repair adds explicit local/staged/shared-header, actual linked-library and Makefile prerequisites to the four reviewed Makefiles; source-only compiler inputs preserve the link recipe. The historical pre-fix run of `tests/test_incremental_dependencies.py` reproduced 16 failures with passing source-change controls; its older blocked rerun was not recovered. Fresh continuation verification: `python3 -m unittest discover -s tests -p test_incremental_dependencies.py -v` passed all six tests, comprising 21 dependency invalidation scenarios across all four targets, with unchanged-state controls (exit 0). It uses the actual Makefiles and real `make -q` queries in disposable timestamp-only fixtures; no compilation, signing or packaging recipe executes. This closes the specified stale-header/linked-library dependency-graph defect. Real clean/incremental iOS output comparison remains a release-validation gap. Systemhook compiles selected libjailbreak sources but does not link libjailbreak.dylib, so that unlinked binary is intentionally not a dependency there.

**Status:** confirmed build-graph gap, inherited across multiple targets.

**Evidence:** `BaseBin/systemhook/Makefile`, `launchdhook/Makefile`, `jailbreakd/Makefile` and `Packages/libkrw-provider/Makefile` list source files but not included headers/generated dependency files as prerequisites. Top-level BaseBin rebuilds its staged headers and libraries, yet an existing target can remain up to date when only a shared structure/RPC declaration changes. Re-signing and copying that binary does not recompile it.

**Consequence:** exactly the ABI/layout edits common in this port can yield mixed-generation archives during incremental iterations. This does not prove any particular installed Wolf archive was stale; that requires its build and binary evidence. Validate a header-only edit and a linked-library API change in a disposable build fixture, and compare clean/incremental outputs before release.

**Historical pre-fix build-graph validation:** copied the baseline Makefiles and source prerequisites into disposable local fixtures, supplied non-executable timestamp-only target/library files, and queried the real make dependency graph with `make -q <target>`. At that baseline all four targets remained "up to date" after the staged shared info.h and linked libjailbreak became newer, but became out of date after a listed source file became newer. No build/sign/package recipe ran. Evidence: `review-evidence/checks/incremental-graph.py` and `.log`. This reproduced the original gap; the new passing fixture run above verifies its dependency-graph repair. A clean/incremental comparison of real iOS archives remains later validation.

### PR-017 - P2 - RPC argument contracts and release diagnostic behavior are not fail-safe

- [x] **Resolution:** the existing shared-dispatcher repair validates required input types before invoking a handler, explicitly describes genuinely optional inputs, separates descriptor indices from native argument slots, bounds the eight-slot call frame, and sends an unsupported-action result. RootHide handlers reject missing/wrong-type blacklist inputs and oversized PIDs, and cap architecture preferences at 64 with fixed-size storage and checked 32-bit fields. Diagnostic requests validate their envelope and reply availability, return ENOTSUP when logging is disabled or an action is unknown, reject missing log/test payloads and arithmetic overflow, and require explicit ENABLE_CRASH_TESTS to enable the root crash test. The historical baselines reproduced 20/34 argument-contract and 19/28 diagnostic failures; their blocked post-fix results remain unavailable and were not recovered. Fresh continuation verification: `python3 -m unittest discover -s tests -p test_rpc_argument_contracts.py -v` passed all 34 scenarios, then passed again with fatal sanitizer diagnostics; `python3 -m unittest discover -s tests -p test_diagnostic_requests.py -v` passed all 28 release/debug scenarios (two separate unittests, ASan/UBSan, exit 0 for each suite). The latter also rejects any captured UBSan runtime diagnostic. These compile actual dispatcher/handler source against real host XPC objects, replacing transport, trust, process and abort operations with local observations. No request, signal or kernel operation is sent to a real process/service. This closes the stated argument/diagnostic source defects, not installed-client ABI compatibility or target service qualification.

**Status:** confirmed inherited robustness defects/footguns. No malformed requests or crash-test commands were sent to a device.

**Evidence:** `libjailbreak/src/jbserver.c:45-93` converts declared argument types to values without enforcing whether required fields were supplied; a missing string reaches the handler as NULL. `launchdhook/src/jbserver/jbdomain_roothide.c:105-113` then passes `checktype` directly to `strcmp`. The recursive trust handler correctly rejects missing principal paths (`32-34`) and checks individual architecture-entry types (`43-47`), but creates two stack arrays from the incoming architecture count without an application-level bound (`36-39`). These distinctions matter: it is not accurate to claim that no validation exists anywhere.

**Additional current-source defect reproduced during implementation:** the original dispatcher advances one shared index for both native argument slots and schema descriptors. A DATA descriptor consumes two native arguments, so it skips the following descriptor. The systemwide trust action declares `caller-token`, `fd`, DATA `siginfo`, then BOOL `attach`; the final flag consequently remains zero in the handler call. A data-output descriptor likewise fails to provide the independent size/tail output addresses. The actual-source fixture reproduced both cases with transport and trust operations replaced by observations. The repair uses separate descriptor/native-slot indices for input, output and cleanup; the older blocked run remains unavailable, while the fresh passing argument-contract run above verifies those slots.

`jailbreakd/src/server.m:158-178` aborts on `JBD_MSG_SYSTEMWIDE_LOG` when `ENABLE_LOGS` is disabled. That is the default in `libjailbreak/src/roothider/log.h`. A logging-enabled client talking to a log-disabled server can therefore terminate the service instead of receiving an unsupported/no-op result. A root test call also deliberately aborts at `server.m:182-189`; this remains an operational crash-test surface, not an unexplained spontaneous-crash diagnosis.

**Required validation:** missing required fields, bounded architecture preferences, incompatible client/server diagnostic configurations, and explicit unsupported-action results. These are useful trusted-environment compatibility checks, not a proposal for a security framework or an adversarial device test.

### PR-018 - P2 - The palera1n helper transport can return success after incomplete pipe I/O

- [x] **Resolution:** client and helper share complete nonblocking pipe transfers with EINTR/short-I/O handling, active-frame deadlines and SIGPIPE suppression while preserving the existing field order. Client callbacks serialize complete requests, bound transfers to 1 MiB frames, propagate operation errors, and close a desynchronized channel without replay. Helper allocation, address/size bounds, complete incoming write payloads and returned Mach read lengths are checked; idle-before-command remains valid, partial commands time out, and EOF/unsupported commands terminate without diagnostic aborts. Shutdown and preflight waiting use a five-second deadline plus a bounded one-second termination/reap grace, retaining an unreaped helper PID rather than claiming it exited. The long-running helper startup now checks spawn-attribute/persona and file-action setup, every pipe and file-action operation, and every setup failure before attempting spawn. All four parent pipes are relocated above the reserved child descriptors before file actions are constructed, preventing the common low-FD 5/6 collision from closing the helper's transport descriptors. TDD reproduced the unchecked setup/collision failures first; fresh `PYTHONPATH=tests python3 -m unittest -v test_palera1n_transport test_palera1n_server_transport test_palera1n_spawn_setup` then passed the 33 client/preflight scenarios, 19 helper-frame scenarios and the eight startup/collision scenarios (three unittests, ASan/UBSan, exit 0). The fixtures use ordinary host pipes or modeled file descriptors and nonprivileged fake children; no kernel primitive or device/service is contacted. This closes the source transport/startup defect, not live palera1n hardware qualification.

**Status:** confirmed inherited transport failure paths, scoped to the palera1n route rather than all Dopamine 3 devices.

**Evidence:** `Application/Dopamine/Exploits/palera1n/palera1n.m:176-222` writes command/address/length/payload fields without checking complete delivery and reads the result only once. Both read/write primitive wrappers initialize `ret` to zero and ignore the outcome of the final read. EOF or a failed result read can therefore return success; an incomplete payload can desynchronize subsequent frames. Waiting for the helper has no deadline. The initial kernel-magic check at `274-280` validates one early operation, not the continued transport contract after the callbacks are installed at `283-284`.

**Required validation:** interrupted and short I/O, helper EOF/exit, helper stall, and partial frame delivery using ordinary host pipes and a nonprivileged fake helper. Concurrent use also needs a defined serialization rule, but this review has not established that current primitive consumers call these callbacks concurrently.

### PR-019 - P3 - Unused upstream process-write wrapper calls itself with a page-table pointer

- [x] **Resolution:** `proc_vwritebuf` now dispatches to `vwritebuf` after resolving the translation table, matching the read wrapper. `python3 -m unittest discover -s tests -p test_process_write.py -v` compiles the actual function with ASan/UBSan and mocked object/memory operations. The pre-fix run reproduced both incorrect recursive-dispatch cases; the post-fix run passed all 10 success, downstream-error and missing-object cases (one unittest, exit 0). No live kernel or process memory was accessed.

**Status:** confirmed dormant upstream Dopamine 3 defect retained byte-for-byte, not an identified cause of the current port failures.

**Evidence:** `BaseBin/libjailbreak/src/primitives.c:194-204` resolves a process's translation-table pointer, then returns `proc_vwritebuf(ttep, ...)` instead of calling the translation-table-level `vwritebuf`. The corresponding read wrapper correctly calls `vreadbuf` at line 191. The recursive call interprets a page-table pointer as a process pointer, so a later guard may fail or the code may follow invalid kernel object addresses; this is not necessarily an infinite recursive loop.

The source-wide caller search found only the declaration, definition and erroneous self-call, with no active Application/BaseBin consumer. Its current priority is preventing future reuse and upstream propagation, not attributing boot or SSH symptoms to an uncalled helper.

### PR-020 - P2 - Exec tracing does not correctly propagate attach failure or preserve exception-port state

- [x] **Resolution:** exec-trace setup now preserves and restores the prior exception-port state on attach failure, returns the real attach outcome instead of a shadowed success value, and releases every saved exception-port right plus the retained task right through one ownership helper. The exception server now replies on pid/thread-state lookup failures, returns failure for unhandled exceptions, releases each received thread/task right, transfers the SIGSTOP task right explicitly into the trace record, and tears down a known trace when state acquisition fails. Cancellation keeps a successful request alive until its exception completes, releases a stale record exactly once when the target is already gone, and rolls back its cancellation state when signaling fails for a retryable reason. The exec-patch kqueue loop retries EINTR and handles other read/filter failures without assertions. Focused TDD verification reproduced the original false-success/state-leak paths before repair, then `PYTHONPATH=tests python3 -m unittest -v test_exec_trace_cancel test_exception_server_ownership test_exec_trace_ownership test_exec_patch_events` passed all four actual-source fixture suites (ASan/UBSan, exit 0). These host fixtures use mocked task/ptrace/kevent operations and send no real exception, signal or task mutation; modern target exception-port eligibility remains V-04 rather than part of this source closure.

**Status:** confirmed inherited source defects; compiler corroboration and API-contract verification completed, not a live exec/ptrace test.

**Evidence:** `libjailbreak/src/roothider/exec_trace.m:251` initializes the function's result to zero. The `int ret` at line 276 shadows it; the failed-attach path sets the inner variable to -1, frees the trace record, and returns the unchanged outer zero at line 298. Compiling the actual file for arm64e with `-Wshadow -fsyntax-only` reports this exact declaration pair. Not all six emitted shadow warnings are bugs; this pair changes the returned outcome.

**Rejected count hypothesis:** the zero-initialized `saved_exception_types_count` at `253-259` initially looked wrong when compared with an old manual and the kernel-side helper. A read-only query of the host process returned the same successful one-entry result with an initial count of 0 and of 14. [Apple's current MIG definition](https://github.com/apple-oss-distributions/xnu/blob/main/osfmk/mach/task.defs) declares the exception arrays as `out` without `CountInOut`, unlike `task_info`. The initial zero is therefore **not included as a demonstrated user-space bug**. Kernel-helper and generated-client contracts must not be conflated. Evidence: `.build/port-review/evidence/validation/exception-port-capacity.log`.

After installing the new exception port, the failed `PT_ATTACHEXC` path does not restore the original handlers (`262-281`). Failures of get/set-exception-ports also leave the allocated trace structure unfreed (`286-298`). The exception-receiver loop does not release the task/thread rights carried by each received request before reusing the message buffer, and its `pid_for_task` failure path skips replying or disposing of the request (`65-89,209-215`). The existing saved-port cleanup at `48-52` is real but covers a different set of rights.

**Related exec-event failure path:** `roothider/exec_patch.m:21-24` asserts that every `kevent` read returns exactly one event, rather than handling an interrupted/failed read. This is a daemon-availability footgun, not proof that an interruption occurred in an earlier trial.

**Required validation:** failed attach must return failure and restore state; every failure before/after exception-port replacement must release its own allocations/rights; cancellation and process exit must complete exactly once. Test new-OS exception-port eligibility separately from the legacy crash reporter, which already has an iOS 17 guard. A passing reporter guard test does not cover this separate backend.

### PR-021 - P1 risk - The remote dyld backend retains unvalidated kernel-layout and loader assumptions

- [x] **Resolution:** the remote-dyld path now validates a unique local `TASK_DYLD_INFO` field pair before reusing its offsets, checks the target's public task-info values against the candidate fields, validates the auxiliary layout where required, verifies each kernel publication write and retains a snapshot for rollback on later failure. The old `loadDyldCache` symbol is no longer a prerequisite for ordinary loader replacement and is required only for the supported iOS 15 arm64e spinlock-only route; CoreSymbolication lookup failures are bounded instead of dereferenced. Both remote branch writers protect and restore the complete 20-byte sequence, preserve the original bytes and attempt rollback on protection/write failure, while thread enumeration now reaches one cleanup path on state-read failure. Fresh `PYTHONPATH=tests python3 -m unittest -v test_remote_dyld_safety` passed all three focused actual-source tests, including full-range protection/rollback, bounded metadata/loader failures and transactional publication ordering. A fresh `gmake -C BaseBin/libjailbreak clean && gmake -C BaseBin/libjailbreak -j4 sign` under the repository tool environment rebuilt and merged both arm64 and arm64e `libjailbreak.dylib` slices successfully; existing qualifier warnings remain. This closes the source-level range, unchecked-publication, unnecessary-symbol and cleanup defects. Exact target task-layout identity, executable-page/PAC behavior and real cross-process loader compatibility remain V-02/V-05 qualification obligations rather than being inferred from host fixtures.

**Status:** confirmed retained assumptions and a concrete protection-range defect; target-build compatibility and actual failing paths remain unverified. The legacy-to-current diff changes diagnostic phases, not these mechanics.

**Kernel publication:** `roothider/dyld_patch.m:99-162` searches only the first 200 eight-byte slots of the local task for adjacent values matching TASK_DYLD_INFO, then scans a target task for the generic six-word pattern `{1,0,0,0,1,0}`. It caches the first match process-wide with `dispatch_once`, clears that 48-byte region in future targets, and publishes the address/size with unchecked kernel writes. Missing offsets abort the hosting process. No type/layout validation or uniqueness check proves what the six-word region represents on a newer kernel. The special allowance for a missing pattern in launchd does not establish that another target will share the same discovery outcome.

**Loader compatibility:** `loadDyldInfo:280-285` requires the old `dyld3::loadDyldCache` mangled symbol for every load, even though the spinlock operation using it is restricted to iOS 15 arm64e (`656-675`). An unrelated full-loader replacement therefore depends on a legacy symbol that it need not use. The helper also calls its dynamically resolved CoreSymbolication function pointers without checking them (`246-256`). This review has not proved that the symbol is absent from the current tested firmware; the verified issue is the unnecessary version-sensitive prerequisite.

**Executable-memory path:** the alternate-PAC entry trampoline and iOS 15 spinlock helper still use their own `vm_protect`/copy/write/RX sequence (`452-586`), separate from the newer hook-provider adapter. Both final old-code protection calls cover `sizeof(void *)` (8 bytes) while their installed branch sequences are 20 bytes (`503,580`). For a sequence crossing a page boundary outside the first eight bytes, the second page can remain non-executable. No current entry address was shown to have that alignment; the source range mismatch is independently real. Newer executable-page policy and cross-process PAC behavior need explicit validation for this path, not an assumption that testing the main hookd adapter covers it.

**Counter-evidence:** this backend checks many task/thread/VM operation results, strips thread-state PAC before inspecting another process's PC, and cleans up thread ports on its normal re-entry path (`724-786`). It must not be described as doing no PAC or lifetime handling. On a `thread_get_state` error, however, the jump at `719-721` skips that thread-array cleanup. Its callers' policy for a partially modified failed child must also be verified before treating every missing rollback as a live-process corruption bug.

**Required adaptation/validation:** exact task-field identity and offset validation per supported kernel, modern loader metadata without unrelated symbol prerequisites, both PAC entry paths, page-crossing code writes, and failure injection at each publication step. None of these device mutations were performed during this audit.

### PR-022 - P2 - Reversed child/parent path arguments lose hidden-root app debugging classification

- [x] **Resolution:** corrected the check-in call to `(procPath, JBROOT_PATH("/Applications"))` and aligned the public declaration's misleading parameter names with the existing `(child, parent)` implementation. The other active call site already uses the correct order. `python3 -m unittest discover -s tests -p test_app_classification.py -v` first reproduced the two hidden-root/alias classification failures and the header-contract mismatch, then passed both tests after the fix (16 behavioral cases, ASan/UBSan, exit 0). It compiles the actual classification block and actual canonical-path helpers, using ordinary temporary files for hidden-root/normal-container apps, aliases, sibling prefixes, an exact root, missing paths and an outward symlink, with the preference enabled and disabled. No kernel flag or credential operation runs; this closes the source classification regression, not target JIT qualification.

**Status:** confirmed port regression, reproduced with the actual path predicate in an isolated local fixture.

**Evidence:** `BaseBin/launchdhook/src/jbserver/jbdomain_systemwide.c:251` calls `isSubPathOf(JBROOT_PATH("/Applications"), procPath)`. The definition at `libjailbreak/src/roothider/common.m:350-362` takes `(child, parent)`, not `(parent, child)`. Legacy roothide's corresponding check-in code (`jbdomain_systemwide.c:301`) uses the correct order. The other branch, `isRemovableBundlePath`, recognizes ordinary UUID application containers, not the hidden branded root.

**Consequence:** a process such as `<jbroot>/Applications/Sileo.app/Sileo` is not recognized by this app classification. `fullyDebugged` remains false even when `markAppsAsDebugged` is enabled (`254-265`). This loses the requested full app-debugging/JIT treatment; it does not mean all code-signing relaxation or injection is absent, because `cs_allow_invalid(proc, false)` still executes.

**Local validation:** compiled the actual `isSubPathOf` implementation against an ordinary temporary Applications/app/executable tree. The legacy argument order returns 1; the current argument order returns 0. Evidence: local `review-evidence/checks/app-path-order.c` and `.log`. Cover normal-container apps, branded-root apps, sibling-prefix paths and the disabled preference in a later check-in integration test.

### PR-023 - P2 - Retaining the legacy libkrw provider crosses a changed gSystemInfo ABI

- [x] **Resolution:** the current libkrw provider package is versioned 2.0.5 in both the bootstrap selector and package control, while the pinned legacy RootHide reference ships provider 2.0.4. The existing Debian-aware package selector therefore treats an installed legacy 2.0.4 provider as stale and replaces it with a provider compiled against the current `system_info` layout; an installed 2.0.5 is retained. A new migration case in the actual package-selection fixture first failed with the prior 2.0.4/2.0.4 version tie (`34 package selection cases, 1 failures`), then `PYTHONPATH=tests python3 -m unittest -v test_package_selection` passed both unittests after the version bump (exit 0). This closes the source migration trigger for the concretely identified incompatible provider. The broader combined package/build-graph rerun was blocked before execution and is not claimed. V-08 remains open for provenance/disassembly of any actually installed historical binary and clean/incremental archive comparison, and no unidentified external legacy client is assumed compatible merely from an unchanged symbol name.

**Status:** confirmed source-derived ABI incompatibility and missing migration trigger; the exact installed provider binary has not been disassembled or queried.

**Evidence:** `Packages/libkrw-provider/src/main.c:37-40` reads `kconstant(base)` directly from the dynamically linked `gSystemInfo` structure. This provider source is unchanged between legacy roothide and the current port, but `BaseBin/libjailbreak/src/info.h` changes the layout: the legacy `kernelConstant.base` offset is 16, whereas Dopamine 3/current port place it at 32. Offset 16 in the new structure is `staticSptmBase`. The provider control version remains 2.0.4 across the transition, and `DOBootstrapper.m:21,528-537,1236-1246` only requests an upgrade against 2.0.3, so an installed legacy 2.0.3 or 2.0.4 provider is not selected for replacement.

**Consequence:** an old provider compiled from that legacy structure and resolved against the new libjailbreak can report the wrong kernel base despite using unchanged C source and an unchanged exported symbol name. Rebuilding the provider in the new source tree is not enough when bootstrap migration leaves the installed package in place. This is distinct from PR-011's Debian comparator and metadata hygiene.

**Local validation:** compiled the actual four `struct system_info` declarations as LP64 layout fixtures. Base offsets are 16/32/16/32 for upstream 2/upstream 3/legacy roothide/current port; structure sizes are 840/1296/920/1352. No hardware or live kernel is involved. These fixtures prove the source ABI transition, not which historical package build is installed on the phone. Migration tests must include an existing old provider, not only a clean bootstrap.

**Additional client ABI obligations established in the latest pass:** the actual legacy/current `jbserver_mach_msg_checkin_reply` declarations (`jbserver.h:69-75` versus `73-80`) keep the same aligned size but shift every fixed string by one byte. A fixture using these declarations and the explicitly modeled Darwin LP64 Mach/signature primitives reports sizes 3,112/3,112 and root offsets 49/50. Decoding a current reply with the old declaration yields an empty root, boot UUID and extension string when forceCSAdhoc is false; when true, the old root begins with byte 1. The same fixture measures trust-file request sizes 88/96, so the new server's minimum-size check (`jbserver_mach.c:110-112`) rejects an old request before constructing its reply. No mixed-generation Mach traffic was sent. Evidence: local `review-evidence/checks/wire-layout.py`, generated C and sanitizer-clean log.

The native `jbclient_process_checkin`/`jbclient_mach_process_checkin` exports gain a fifth output-pointer argument; `jbclient_trust_file` gains an attach flag. These prototype changes are visible in the legacy/current public headers, while the new implementation consumes the extra arguments (`jbclient_xpc.c:133-140,169-181`). Old dynamically linked callers therefore need explicit migration or compatibility treatment; the unchanged C symbol names do not make their calling conventions compatible. Current matched app/basebin sources were rebuilt together, and this review has not identified a particular installed external old client calling those functions. These wire/native differences are confirmed compatibility boundaries, not additional demonstrated device crashes or proof that every retained package uses them.

### PR-024 - P1 conditional - Hookd resumes a target even when restoring executable permission failed

- [x] **Resolution:** `apply_hook` now derives and validates the complete page-aligned protection interval before acquiring the target task, including address/rounding overflow rejection. It suspends the target, makes the whole affected interval writable, writes only the requested bytes, and restores RX across that same full interval. If RX restoration fails, hookd no longer resumes a target that may execute from an NX page: it requests `task_terminate` and releases its task right; if termination itself fails, that failure is returned and the hookd-added suspend remains in place rather than resuming corrupted executable state. The focused actual-function fixture was changed first and reproduced the old failed-RX resume path, then passed after the repair; a second red cycle reproduced the non-page-aligned protection range and now covers a write crossing two pages plus address-wrap rejection. Fresh `PYTHONPATH=tests python3 -m unittest -v test_hookd_patch.HookdPatchTests.test_target_threads_and_failure_cleanup` passed under ASan/UBSan, and `make -C BaseBin/hookd hookd` rebuilt the arm64/arm64e daemon successfully. Earlier in this continuation the broader hookd patch/client/server suites also passed after the terminal-path change (four unittests, exit 0). No real task was suspended, protected, terminated or resumed; actual kernel refusal behavior remains device qualification rather than a source-level unsafe-resume defect.

**Status:** confirmed failure-state gap in the current writer; not an attributed cause of a recorded panic.

**Evidence:** `BaseBin/hookd/src/main.c:49-59` suspends the target, changes the affected pages to writable/non-executable, writes the patch, and attempts to restore read/execute. If that restoration fails, it retains the error but still calls `task_resume`. The new suspension policy is a real improvement over the upstream writer's unprotected interval; it does not establish a safe end state after a failed permission restoration.

**Consequence:** a live process can resume with the affected code page still non-executable or otherwise incorrectly protected. Returning an error to the requester is not sufficient when another target thread, or the requester itself on return from the kernel, executes that page. This is especially consequential for shared syscall wrappers and PID 1. Conversely, simply leaving a target suspended forever is not a satisfactory recovery policy either; the failure contract must define a recoverable prior state or an explicit terminal outcome.

**Counter-evidence/validation:** the original fixture proved the unsafe state by observing a resume after injected RX-restoration failure. The repaired fixture now requires termination instead of resume, exercises successful and failed terminal cleanup, preserves balanced suspend/resume on recoverable write/protection failures, covers a two-page write interval and rejects address overflow before acquiring a task right. It remains a mocked Mach environment and therefore does not prove a particular iOS kernel will refuse or accept any protection transition.

### PR-025 - P2 - Loader-hook installation can fail while initialization continues as though the required hooks exist

- [x] **Resolution:** loader-hook installation now has an explicit failure contract. `init_dyldhooks` returns failure when neither loader interface exists and aggregates all four dlopen-family backend results instead of discarding them; `roothide_init_with_checkin` stops before path redirection/roothideinit loading when those required hooks fail. The dlsym hook is installed through a checked helper and constructor initialization stops when libsandbox/sandbox_apply or the required dyld hook is unavailable. Both vtable backends now report failed protection restoration instead of returning success. Fresh verification on 2026-09-16: `PYTHONPATH=tests python3 -m unittest -v test_dyld_fallback test_dyld_installation_failures test_dyld_dispatch` passed all 17 focused tests, covering legacy/modern/missing loader layouts, arm64 and arm64e dispatch, injected backend failures, check-in gating, dlsym installation and writable/restore protection failures. `make -C BaseBin/systemhook -j4` then completed with exit 0; the build emitted existing syscall-macro and nested-pointer-qualifier warnings but no error. This closes the source-level false-success initialization defect; real installed dyld-stub/PAC/protection-policy compatibility remains a separate runtime qualification gap.

**Status:** confirmed error-propagation/incomplete-initialization defect, including the initial dirty loader implementation; actual installed stub compatibility remains a separate question.

**Evidence:** `BaseBin/systemhook/src/roothider_main.c:479-507` installs four dlopen-family hooks but neither aggregates nor propagates their results. Missing interfaces also fall through without failure. `roothide_init_with_checkin:534-545` continues to path redirection and loading roothideinit regardless. The dlsym hook at `systemhook/src/main.c:458-471` similarly ignores installation failure. In the vtable backend, `hook_dyld_routine:465-473` additionally returns success after a failed protection-restoration call.

**Consequence:** an unexpected dyld stub, rejected page transition or unavailable interface can leave only some late-load operations intercepted. A successful systemhook check-in or constructor completion therefore does not prove that libraries loaded later receive recursive trust. Temporary sshd-only diagnostics reveal some failures for that process but do not establish a general initialization contract. This is not evidence that the current five captured stubs are decoded incorrectly.

**Positive validation:** the actual uploaded `dyld_dispatch.c` and existing dispatch fixture pass all six local modes (captured, changed, invalid, runtime, backend-failure, exports-only), under a mock Mach ABI with ASan/UBSan. The matcher rejects changed instructions; the runtime entry lookup correctly uses public `dlsym`, and the backend propagates its own error. Those are useful protections at the lower layer, but their callers discard the result. The Linux fixture does not execute ARM instructions, authenticate PAC, or exercise real memory-protection policy.

### PR-026 - P2 - Hookd leaks each reply allocation and does not bind reply counts to request/output counts

- [x] **Resolution:** the client now validates counts, pointers and bounded frame-size arithmetic before encoding or sending; reply decoding requires exactly the requested result counts, validates the complete extent and integer range, and leaves all caller outputs untouched on rejection. Packed records are copied through aligned local structures without changing the wire layout. The server validates complete hook/fixup framing and the rounded, bounded audit trailer before invoking any operation, checks both receive/reply allocations, allocates one complete reply before processing the batch, and frees it on success and send/task refusal. Existing caller admission, operation callbacks and their raw results are preserved; `apply_hook` and `apply_fixup` were not changed. Fresh `python3 -m unittest discover -s tests -p test_hookd_protocol.py -v` reproduced 17 failing scenarios out of 26 before repair, then passed all 26. Fresh `python3 -m unittest discover -s tests -p test_hookd_server_protocol.py -v` reproduced 16 failing scenarios out of 24, including reply leaks, allocation-refusal crashes and unaligned reads, then passed all 24 after repair. Both actual-source fixtures use ASan/UBSan with fatal sanitizer diagnostics; the server suite includes 32 repeated requests, odd payloads, malformed tails, allocation refusal, send/receive/task errors and balanced owned allocations/rights. Transport, task lookup and operation callbacks are local observations: no real message, task mutation or device operation occurs. This closes the source-level framing/ownership defect, not PR-024's executable-page recovery or real Mach/kernel qualification. The subsequent Git diff/whitespace-check command was blocked and was not rerouted; no full-tree build or diff-check success is claimed.

**Status:** confirmed upstream-3 defects inherited by the new RootHide service dependency. The allocation leak occurs on normal requests; the count mismatch requires an inconsistent reply and is not asserted to occur with every valid current request.

**Evidence:** `BaseBin/hookd/src/main.c:174-206` allocates a reply, sends it, destroys the incoming message and loops without freeing the reply allocation. Freeing the temporary result arrays at 181-182 does not free that buffer. Separately, `libjailbreak/src/hookd.c:61-77` validates reply-internal size arithmetic but copies as many results as the reply declares, without receiving expected request counts or destination capacities. `hookd_send_requests:80-101` has those counts but does not pass them to the decoder. Single-operation wrappers initialize their result to zero (`112-115,125-128`), so a zero-result reply can look like a successful operation.

**Local validation:** the actual decoder accepts a zero-result reply for a modeled one-operation call; a two-result reply overwrites the adjacent sentinel in a two-element observation buffer when the logical output capacity is one. No malformed messages were sent to any daemon. The server's partial-frame loop (`main.c:143-153`) also lacks a complete-header check before reading dataSize; trailer positioning uses the unrounded message size (`110`), while arbitrary hook payload sizes are admitted by the encoder. Those framing cases require protocol fixtures rather than assuming all future payloads have current instruction alignment.

**Required adaptation:** balance normal allocation ownership; require exactly the requested result cardinalities and a defined outcome for incomplete batches. These are internal service correctness contracts, not a proposal for an elaborate adversarial-validation framework.

### PR-027 - P2 - Stock-dyld identification treats the binary UUID as a terminated string

- [x] **Resolution:** the existing repair uses a four-byte `memcmp` for the marker, so no string operation scans the 16-byte binary UUID. Fresh verification: `python3 -m unittest discover -s tests -p test_dyld_uuid.py -v` passed all seven actual-parser binary-marker/check-in parsing cases (one unittest, ASan/UBSan with fatal sanitizer diagnostics, exit 0). It covers nonterminated and embedded-zero stock UUIDs, a matching marker and missing header/UUID/section/state controls; image and section access return local fixture data and no check-in or loader modification occurs. The older blocked pre-fix request remains unavailable. This closes the UUID read-boundary defect, not runtime check-in, section-layout or capability qualification.

**Status:** confirmed out-of-bounds read, reproduced under AddressSanitizer; inherited from upstream 3 and exercised more broadly by RootHide's stock-loader fallback mode.

**Evidence:** `BaseBin/systemhook/src/main.c:344-347` fills a 16-byte `uuid_t` and passes it to `string_has_prefix`. The shared helper (`systemhook/src/common/common.c:23-36`) calls `strlen(str)` before comparing the prefix. A UUID is not a NUL-terminated C string. The generated Dopamine UUID contains a deliberately written terminator, but a stock UUID does not have that guarantee. `roothide_init:526-530` also probes this path before the constructor's check-in probe.

**Local validation:** a 16-byte nonzero UUID passed to the actual prefix helper produces an ASan stack-buffer-overflow in strlen (17-byte read at the end of the array). This does not establish that the current phone's UUID/adjacent stack happens to fault; it proves the API-contract violation. Test both a stock binary UUID and the generated DOPA marker with bounded comparisons.

### PR-028 - P2 - The hookd entitlement probe uses a big-endian blob length without conversion

- [x] **Resolution:** the repair decodes lengths from big-endian, checks allocation/returned extents, validates the complete blob magic and rejects non-dictionary plist results with balanced cleanup. The real ERANGE size response supplies a valid length without a complete magic header, so the preliminary request intentionally validates only the decoded extent while complete-blob validation remains mandatory on the second request. Fresh `PYTHONPATH=tests python3 -m unittest -v test_entitlement_blob test_entitlement_blob_host test_entitlement_blob_debugger` passed all three suites: the modeled actual-function fixture covers the byte-order/extent/error matrix; the signed self-process controls exercise real `csops` plus the real XPC parser; and the new debugger-positive control runs Xcode's Apple-signed `debugserver`, whose real 399-byte entitlement blob contains `com.apple.private.cs.debugger=true`, through the exact production `copy_entitlements_xpc`/`process_requires_hookd` source and verifies that the hookd path is not required. Target qualification then ran the exact production parser in a temporary developer-signed probe on the connected iPhone12,1 running iOS 26.0 build 23A341. Its real profile carries `get-task-allow=true` but not Apple's private debugger entitlement; the probe reported `PR028_IOS_OK debugger=0 get_task_allow=1 requires_hookd=1` and exited 0. The original Wolf 24 app was immediately restored without launch. This closes the byte-order/size-probe defect and the previously stated real-debugger/target-iOS validation gap for this finding; it does not imply every private entitlement or every OS build has been qualified.

**New host evidence and limits - updated 2026-09-17:** before the repair, the signed empty-dictionary helper returned `queries=1 result=-1 errno=34 magic=00000000 first=192 returned=0`. Afterward its actual dictionary matched the signed empty dictionary and the second request used the decoded allocation capacity. The no-entitlement control remains a one-query NULL result. Earlier custom-key helpers were rejected by macOS AMFI as ad-hoc signatures containing restricted entitlements; subsequent sandbox-restricted helpers terminated with SIGTRAP without an established parser outcome. Those artificial signatures remain excluded rather than weakening host protections. The later Apple-signed Xcode `debugserver` control supplies the real restricted debugger entitlement without altering AMFI policy, and the separate iPhone probe supplies the target-iOS parser/control-flow observation. These results qualify the repaired parser contract while preserving the earlier failed-signature evidence as a limitation of synthetic private-entitlement tests.

**Status:** confirmed upstream-3 parsing defect inherited by the port; platform contract cross-checked separately against Apple's published XNU source.

**Evidence:** `BaseBin/systemhook/src/main.c:268-300` reads a `CS_GenericBlob` header through CS_OPS_ENTITLEMENTS_BLOB, then uses raw `hdr.length` for allocation, the second csops request and the plist extent. There is no byte-order conversion. Apple's [csops_copy_token implementation](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/kern/kern_proc.c) writes the short-buffer length with htonl; its entitlement handler converts the XML blob length with ntohl before copyout. This outside source verifies the wire contract, not every target firmware's implementation.

**Consequence:** on the little-endian targets, the probe can request an inflated allocation and parse bytes beyond the actual returned plist. Allocation or parse failure defaults `process_requires_hookd()` to true (`312-319`), potentially defeating its intended debugger-entitlement exception. The real XPC parser's exact reaction to excess bytes and the observed memory cost remain runtime questions, not established crashes.

**Local validation:** the actual function with a csops fixture following that byte-order contract receives a 256-byte blob, requests 65,536 bytes and passes 65,528 bytes to the parser instead of 248. No large device allocation was attempted. Exercise real signed debugger/non-debugger entitlements and empty/error results after correcting the interpretation in a later implementation phase.

### PR-029 - P2 - External syscall-hook publication is neither failure-atomic nor capacity-bounded

- [x] **Resolution:** the external hookd adapter now validates full-width, instruction-aligned ARM64 branch displacements before encoding; writes each trampoline body before publishing the reachable branch; rejects a scan whose required trampoline count cannot fit in the allocated 0x4000-byte page; and propagates allocation/emission failures instead of silently returning success. The private `__pthread_create` trampoline setup now returns a checked status, refuses a five-instruction prologue containing PC-relative control/data references, checks both VM allocation and both hook installations, and releases its trampoline page on setup failure. Both systemhook and launchdhook require this setup to succeed before continuing their iOS 19+ hookd initialization. The new focused regression suite first reproduced the out-of-range branch acceptance, branch publication after body-write failure, over-capacity scan/ignored emission failure, and unchecked/private-prologue setup paths; fresh `PYTHONPATH=tests python3 -m unittest -v test_hookd_external_safety` passed all four ASan/UBSan-backed actual-source tests. Fresh `make -C BaseBin/systemhook -j4 && make -C BaseBin/launchdhook -j4` then rebuilt both arm64/arm64e consumers successfully (exit 0), with only pre-existing macro/pointer-qualifier warnings. This closes the source-level publication/capacity/setup defect; actual Frida/ElleKit executable-page/PAC behavior remains part of V-02 target qualification.

**Status:** confirmed defects in `BaseBin/systemhook/src/common/hookd_external.c`, retained byte-for-byte from upstream 3; applicability is the external inline-syscall adaptation route, not every ordinary hook.

**Evidence:** `emit_hookd_svc_trampoline:83-84` attempts to redirect the original instruction even when writing its backing trampoline failed, because both calls execute before their errors are ORed. `apply_hookd_syscall_patches:91-112` allocates one 0x4000-byte trampoline page, grows a word offset for every matched syscall without a capacity check, ignores individual emission failures and still returns success. The four-instruction template is 16 bytes (`hookd_external.S`), allowing 1,024 entries in that allocation.

**Local validation:** the actual emitter, with branch encoders and memory writes mocked, publishes the original branch after a failed body write (`result=5 writer_calls=2 branch_published_after_failed_body=1`). The actual scan function accepts 1,025 modeled syscall sites and reports success while its final 16-byte body starts at offset 16,384, outside the allocated page. No executable memory was patched.

The branch encoders at `26-64` also narrow the 64-bit displacement to int32 before validating it. A source-derived fixture shows a target 16 GiB away being accepted as an instruction branching to the origin. That particular distance is not established for a current target image; nearby-allocation checks do not validate the independently located interceptor target.

**Required adaptation:** validate complete trampoline construction before publishing a reachable branch, carry errors to initialization, bound or grow storage, and check the full-width displacement before narrowing. The private pthread prologue trampoline at `223-240` likewise needs checked allocation/hook outcomes and verified PC-independence rather than an assumed fixed prologue.

### PR-030 - P2 - External thread interception shares one start-routine pointer across distinct threads

- [x] **Resolution:** replaced the shared routine pointer with a separately allocated routine/argument context for each intercepted creation. The child copies and releases its context before scanning; failed creation releases it in the parent; allocation refusal returns ENOMEM without starting a thread. Unintercepted calls remain direct. `python3 -m unittest discover -s tests -p test_thread_start_ownership.py -v` first reproduced wrong-routine dispatch in both delayed-start orders, then passed all seven ownership cases after the fix (one unittest, ASan/UBSan, exit 0). The actual wrappers and original-call declaration execute with queued/immediate start observations and creation/scan stubs; arguments, return values, allocation lifetime and positive error codes are checked. No target thread or hook is created. This closes per-creation routine ownership, not PR-029's independent private-prologue installation/ABI qualification.

**Status:** confirmed upstream-3 lifecycle defect inherited unchanged; a deterministic delayed-start fixture reproduces the mismatch without creating or instrumenting a real target thread.

**Evidence:** `BaseBin/systemhook/src/common/hookd_external.c:184-210` saves each intercepted `start_routine` into global `gOrigThreadRoutine`. The new thread receives only the original argument. Its wrapper first scans/patches images, then calls whichever function pointer is currently in that global. A second intercepted creation can replace the function before the first wrapper reaches it.

**Local validation:** the actual `_pthread_create_hook` is called twice against a mock that delays both starts. Invoking the queued wrappers yields `B with A argument` followed by `B with B argument`. This is a valid delayed-start ordering; reproducing it does not depend on a C data race or on two calls occurring at exactly the same time.

**Required adaptation:** associate the original routine and its argument with each individual created thread, including ownership on creation failure. Restricting interception by TPIDRRO_EL0 does not provide per-thread argument storage or serialize delayed starts.

### PR-031 - P2 - The credential donor drops privilege before constructing distinct non-root real/effective UIDs

- [x] **Resolution:** the donor now installs the requested real/effective UID pair in a single `setreuid(ruid, uid)` operation after completing group setup, instead of dropping all UIDs to the target effective UID before attempting the real-UID change. `python3 -m unittest discover -s tests -p test_donor_identity.py -v` reproduced six failed assertions in three distinct-ID cases, then passed all 11 cases after the repair (ASan/UBSan, exit 0). The fixture executes the actual identity-operation sequence against the report's UID/group permission model, covering effective-root, equal IDs, distinct non-root IDs, root real/non-root effective IDs, saved IDs and five independently refused operations. No host or target identity is changed. This closes the operation-order defect; the independent group-count transport defect remains PR-032, and actual target credential equality remains part of section 6. A fresh upstream-source fetch was blocked and was not rerouted; this fix relies on the source and primary API contract already documented in this finding, not a claimed new fetch or target-kernel test.

**Status:** confirmed source/operation-order defect inherited from the Dopamine 3 donor design. The port correctly detects the failed identity calls, but does not make the requested identity constructible.

**Evidence:** `BaseBin/dyldhook/src/main.c:171-181` performs the group changes, calls `setuid(uid)` twice, then calls `setreuid(ruid, -1)`. A privileged donor requested to have effective UID 502 and real UID 501 first changes all three UIDs to 502; its subsequent attempt to restore real UID 501 is no longer privileged. Apple's published [setuid/setreuid implementation](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/kern/kern_prot.c) confirms these permission and saved-ID rules. This external contract check is distinct from an on-device test.

**Reachability/consequence:** the check-in handler reads the preexisting real UID, replaces the effective UID with a set-ID executable's owner and sends both to `proc_ucred_update_content` (`jbdomain_systemwide.c:267-292`). Thus a non-root-owned setuid executable can request this distinct-ID case. The checked port sends a failure marker and rejects check-in; upstream's unchecked version can instead report success with the wrong real UID. Removing the checks is not an adaptation. The common effective-root case is not broken by this particular ordering.

**Local validation:** extracted the actual seven identity operations into `review-evidence/checks/donor-identity-model.c`, replacing syscalls with a small state model of the cited UID rules. The distinct-ID case reports `result=-1 errno=1 ruid=502 euid=502 suid=502 wanted=501/502`; the effective-root control reports `result=0 ruid=501 euid=0`. ASan/UBSan report no fixture errors. This validates the sequence against the modeled contract, not target-kernel execution. Later tests must cover effective-root, equal IDs, distinct non-root IDs, saved IDs and set-ID ownership changes.

### PR-032 - P2 - Credential transfer discards the supplementary-group count and interprets unused slots as groups

- [x] **Resolution:** credential metadata and its serialization now include `ucred.ngroups`; the reader uses a checked 16-bit read, rejects missing/invalid counts, copies only active entries and publishes no partial output on failure. Both systemwide callers use a counted update interface. The legacy public update entry point reads the authoritative count rather than guessing from its supplied array. The donor wire format explicitly includes `--ngroups`; its parser requires complete, nonduplicated decimal fields, bounded group count and matching primary group before any identity operation. The sender does not serialize stale trailing slots. The pre-iOS-17 update path preserves the explicit active group array/count too. `tests/test_credential_group_count.py` originally reproduced nine failing sender/donor scenarios out of ten and two missing-reader/integration tests. Fresh verification in the current continuation ran all three group-count tests successfully, including the 16-scenario counted reader and both caller/legacy-entry checks. A broader credential run initially exposed a stale test-fixture extraction that accidentally included PR-035's owned-attribute cleanup; the fixture was narrowed to the persona result block and its focused test then passed. The full `test_credential_group_count`, `test_credential_helper`, `test_credential_service` and `test_donor_identity` bundle subsequently passed all 12 unittests (exit 0), with the reader reporting 16 scenarios and zero failures. No real credential changes, kernel reads/writes or process signals were issued. This closes the counted supplementary-group transport/source contract; actual target credential equality remains V-03.

**Status:** confirmed missing information in the Dopamine 3 credential interface retained by the port; the effect on a given process depends on its actual unused group slots.

**Evidence:** the check-in and persona handlers copy all `NGROUPS_MAX` group slots from the kernel credential (`jbdomain_systemwide.c:273-274,556-557`). `target_proc_with_ucred` serializes the whole array (`libjailbreak/src/util.c:1095-1103`) without a group count. The donor infers the length by searching for -1 (`dyldhook/src/main.c:174-178`). Neither the callers nor the transported interface establishes that sentinel. The source's `system_info.ucred` metadata also has no group-count member.

**Contract/counter-evidence:** Apple's published [credential implementation](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/kern/kern_credential.c) represents the length explicitly as `cr_ngroups`, copies only the counted entries on a group change, and has zero-initialized credential templates; it does not establish a universal -1 terminator in unused slots. This does not assert that every current target credential has zero-filled tails, or that group-array offsets themselves are wrong.

**Implementation contract check:** Apple's [credential declaration](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/sys/ucred.h), retrieved through `gh` in Codexify, defines `cr_ngroups` as `u_short`, with a separate padding field before the group array. The new metadata sets it at `ucred_cr_posix + 0xC`, alongside the existing `groups` offset at `+0x10`, and the reader fixture deliberately fills padding with nonzero bytes to detect an incorrect four-byte count read. This source contract is not a target-device layout qualification. Coordinated component rebuilds must include the changed metadata and counted donor arguments; an older metadata snapshot missing the new field is rejected rather than guessed.

**Consequence/validation:** a valid one-group representation `{501, 0, ...}` with count 1 becomes a 16-group request including group 0. A shortened group list with old values in unused slots can likewise regain groups that are no longer active. The same extracted donor fixture reports `result=0 groups=16 first=501 second=0`; the -1-filled control reports one group. The existing `credential_helper_tests.c` initializes every unused slot to -1, so it cannot detect this missing count. Preserve an explicit count across the boundary and validate effective-group placement separately; do not assume bounded helper transport establishes credential equivalence.

### PR-033 - P1 conditional - Primitive handoff can report success and end its donor after acquisition failed

- [x] **Resolution:** primitive handoff now has an explicit failure contract on both sides of the userspace-reboot transition. Launchd's recovery path rejects primitive acquisition failure before sending completion, propagates a failed completion message, and only waits/reaps the donor after successful acquisition/completion. The standalone boomerang likewise refuses to notify the old launchd when acquisition fails and propagates a failed completion send before patching the replacement launchd. Stashing now returns an error, checks port/source/spawn-attribute/spawn setup, replaces the unbounded completion wait with a ten-second timeout, terminates/reaps a still-running donor on timeout, and only publishes `BOOMERANG_PID` on success. The re-exec spawn hook keeps hookd alive until stashing succeeds and aborts the re-exec attempt on stash failure rather than tearing down the provider first. A new focused suite was written before the production repair and initially failed all three original assertions: acquisition/completion errors were ignored and teardown preceded stash. After repair, `PYTHONPATH=tests python3 -m unittest -v test_primitive_handoff` passed four tests, including actual `boomerang_recoverPrimitives` execution under ASan/UBSan plus source-contract checks for the standalone donor, teardown ordering and bounded lost-completion handling. A first direct launchdhook build exposed the newly required `signal.h` include plus pre-existing stale staged headers; after adding the include and rebuilding through the top-level dependency/staging target, `make -C BaseBin launchdhook boomerang -j4` rebuilt current libjailbreak, launchdhook and boomerang for arm64/arm64e successfully (exit 0, existing unrelated warnings only). No process was spawned, killed, re-executed or handed real primitives in the fixture. This closes the source-level false-success/unbounded-wait/teardown-ordering defect; live interrupted userspace-reboot recovery remains V-07.

**Status:** confirmed error-propagation defects inherited from upstream 3, with consequences amplified by RootHide's explicit suspended-launchd restart path.

**Evidence:** `launchdhook/src/boomerang.c:92-108` discards the return from `jbclient_initialize_primitives_internal`, sends the completion message, and returns 0. The called function explicitly returns -1 when system-info or physical-RW acquisition fails (`libjailbreak/src/main.c:13-46`). The standalone boomerang repeats the same unchecked acquisition before notifying the old launchd and patching the new one (`boomerang/src/main.c:63-75`). A failure is therefore not conveyed to `launchdhook/src/main.m:155-161`'s apparently protective recovery-result check.

**Related transition:** `boomerang_stashPrimitives` returns void after a spawn failure (`boomerang.c:48-50`). `spawn_hook.c:122-143` cannot observe that failure and proceeds toward launchd re-exec; hookd has already been torn down at line 109. Failed completion delivery is also ignored before a potentially blocking wait for boomerang exit. This is a different failure boundary from the later task-resume fix, which does check its result.

**Local validation:** compiled the actual recovery function with non-device transport/primitive stubs. With primitive acquisition returning -1, it reports `acquisition_result=-1 recovery_result=0 done_sent=1`; the successful-acquisition control also completes. ASan/UBSan are clean. The fixture establishes false success and premature completion in this control flow, not a live primitive-transfer failure or its exact panic signature.

**Required adaptation:** carry acquisition and completion failures through both sides of the handoff, verify required primitives before ending the donor, and define what may happen after teardown but before re-exec. Test spawn refusal, donor early exit, negative acquisition, lost completion and failure after the old process has committed to restart.

### PR-034 - P2 - The new F_ADDFILESIGS_INFO case can return success without its required signature information

- [x] **Resolution:** the hook zero-initializes its signature request and copies only the 24-byte legacy input prefix instead of reading the entire modern 56-byte structure. After successful normalized attachment, RETURN/INFO reissue the original syscall against the accepted kernel blob to obtain its actual end offset and command/extent-specific signature outputs; other commands no longer mutate an output field they do not own. The original argument and slice offset remain intact for that query, and a query failure remains a syscall failure. The installed SDK and XNU handler at `f6217f891ac0bb64f3d375211650a4c1ff8ca1ea` were inspected through Codexify/gh: the kernel reads 24 input bytes for legacy commands, 32 for INFO, and writes full INFO outputs only when its advertised size permits. `tests/test_fcntl_outputs.py` originally reproduced 11 failing scenarios of 14, including six ASan heap overreads on exact 24/32-byte allocations plus missing/incorrect output and retry-error handling. Fresh current-source verification now passes `PYTHONPATH=tests python3 -m unittest -v test_fcntl_outputs test_fcntl_status`, and `make -C BaseBin/dyldhook -j4` rebuilds all six arm64/arm64e iOS15, 16-17 and 18+ variants with every self-contained-symbol check passing (exit 0). The existing `spinlock_fix.c` unknown-`optimize`-attribute warning remains unrelated. This closes the command extent/output contract; live kernel caller behavior remains part of target validation rather than this source defect.

**Status:** confirmed incomplete adaptation in the port's added command case. The affected branch requires a checked-in process and a signature needing TXM flag normalization; ordinary successful kernel handling returns early and is not affected.

**Evidence:** `BaseBin/dyldhook/src/lv_bypass.c:182` adds F_ADDFILESIGS_INFO to the existing signature-command adapter. After normalization and successful server-side attachment (`263-281`), the adapter emulates only `fs_file_start` and returns without retrying the original operation or supplying the INFO outputs. Apple's published [fcntl structure](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/sys/fcntl.h) and [command handler](https://github.com/apple-oss-distributions/xnu/blob/main/bsd/kern/kern_descrip.c) define additional CDHash and hash-algorithm outputs when the supplied structure is large enough. Sharing the input prefix does not make INFO and RETURN output-equivalent.

**Local validation:** compiled the actual hook with non-device signature, transport and Mach stubs. A modeled initial kernel refusal followed by successful normalized attachment reports `result=0 raw_calls=1 attached=1 returned_start=4096 hash_type=-7 unchanged_cdhash_bytes=20`: every INFO output sentinel is untouched despite success. The unchecked-in control preserves the original failure. ASan/UBSan are clean. This is an adapter-contract test, not evidence of a particular dyld caller consuming those fields on the phone.

**Required adaptation:** preserve each command's documented input/output extent and populate outputs from the accepted signature, or obtain them through the original command after attachment. Verify RETURN, INFO with legacy/full structure extents, F_ADDSIGS and failure results independently; do not assume accepting a new command number completes its port.

### PR-035 - P2 - The persona workaround permanently mutates reusable spawn attributes

- [x] **Resolution:** each call now owns its descriptor, attribute bytes and persona payload, with both the attribute pointer and kernel-facing persona descriptor directed at that private copy. The original caller state is untouched during and after success/failure/interleaved calls; the shared wrappers pass the copied descriptor only to the actual spawn invocation. Copy extents and allocations are checked, get/set-flags failures stop before spawn, and temporary copies are released on all returning paths. Jetsam scaling applies once per call and clamps overflow/infinity instead of converting an out-of-range double to int. `python3 -m unittest discover -s tests -p test_spawn_attribute_ownership.py -v` reproduced 19 failing scenarios of 21 (including two UBSan float-to-int findings), then passed all 21 after repair (one unittest, ASan/UBSan, exit 0). Coverage includes reuse, deterministic interleaving, persona/spawn/resume refusal, allocation/size/flag failures, descriptor-only persona, legacy/root/setexec routes, caller-owned suspension and execve return. Tests compile the actual three shared functions and source-private structures with simulated process/credential operations; actual kernel/private-ABI qualification remains in section 6. The independently pending PR-012 fixture was kept scoped to configuration functions when this new helper was inserted; its blocked verification was not rerun.

**Status:** confirmed upstream-3 behavior retained in the shared port path; reproduced against the actual shared spawn function with mocked process operations.

**Evidence:** `BaseBin/systemhook/src/common/common.c:268-293` changes requested persona UID/GID 0 to 501 and adds START_SUSPENDED directly to the supplied attribute object. Its completion path (`358-374`) fixes and resumes the first child but restores neither the persona fields nor the original flags. Apple's published [posix_spawn wrapper](https://github.com/apple-oss-distributions/xnu/blob/main/libsyscall/wrappers/spawn/posix_spawn.c) passes the caller's attribute/persona objects in the syscall descriptor; they are not disposable private copies at this interception point.

**Consequence:** reuse of the same root-persona attributes makes the second call appear to request UID 501, so the workaround is no longer triggered. The old START_SUSPENDED flag can also keep that second child stopped. The port's inner RootHide wrapper restores flags to the value it received, which is already modified by this shared outer function; it does not repair the original caller's state. The jetsam branch likewise multiplies supplied limits in place (`239-253`), so repeated reuse can compound that separate modification.

**Local validation:** compiled the entire `spawn_exec_hook_common` with Blocks support, simulated the iOS 17.6+ branch, and invoked it twice with one attribute object. First call: `result=0 effective_uid=0 resumed=1 remaining_flags=0x80 remaining_persona_uid=501`. Second call: `result=0 effective_uid=501 resumed=0`; only one persona-fix RPC occurred. The fixture performs no real spawn, signal or credential change and is clean under ASan/UBSan. Validate success, spawn failure, persona failure, caller-requested suspension and repeated use with a private per-call attribute snapshot or complete restoration.

### PR-036 - P2 - The newer IOSurface allocator converts an oversized allocation refusal into success

- [x] **Resolution:** the unsigned-address backend now returns zero when the request exceeds 0x10000, matching both public wrappers' failure contract. `python3 -m unittest discover -s tests -p test_iosurface_allocation_result.py -v` first reproduced six false-success cases through the global/local wrappers and the provider's actual initializer-selected allocation entry, then passed all 17 cases after the fix (one unittest, ASan/UBSan, exit 0). The actual backend/wrappers/public dispatcher/provider initializer are compiled with modeled OS availability and mocked IOSurface/memory operations. Tests cover 16, 65,536, 65,537 and maximum-uint64 sizes, a zero-address backend result, unchanged output on refusal, detach behavior, and preservation of the legacy route. No kernel allocation is performed; this closes the refusal-result defect, not general IOSurface backend qualification.

**Status:** confirmed upstream-3 defect retained unchanged. It affects consumers selecting the IOSurface allocation backend; the current ordinary trustcache paths do not reach this size limit.

**Evidence:** `BaseBin/libjailbreak/src/primitives_IOSurface.m:259-261` returns -1 from an unsigned-address function for requests larger than 0x10000. Both public wrappers (`334-364`) treat every nonzero address as success and return that value through the output pointer. The newer initialization registers those callbacks on iOS 16+ (`383-385`), whereas the older baseline did not register its allocator there. `primitives.c:395-409` forwards the result without another size/address check, and the libkrw provider exposes `kalloc`.

**Local validation/counter-evidence:** the actual backend and wrappers, with the iOS 16+ branch selected and IOSurface operations mocked, return `result=0 addr=0xffffffffffffffff allocator_entered=0` for a 65,537-byte request. The 65,536-byte control reaches the allocator. No kernel memory is accessed. `trustcache_file_upload` explicitly caps its request at 0x4000 (`trustcache.c:330-331`), and dynamic trustcache growth also allocates 0x4000 (`171`), so this is not a demonstrated bootstrap/trustcache failure. Keep the oversized-backend result consistent with the public allocator contract and test callers through the exported provider.

### PR-037 - P2 - launchd's automatic-port retry counter wraps beyond its declared range

- [x] **Resolution:** both IPv4 and IPv6 retry counters now use `unsigned int`, so incrementing past 65,535 terminates the loop instead of requesting port zero. `python3 -m unittest discover -s tests -p test_bind_port_range.py -v` first reproduced the 16,385th, out-of-range request in both families, then passed all 11 actual-source cases after the fix (one unittest, ASan/UBSan, exit 0). Exhaustion now returns the final `EADDRINUSE`; first/last-port success, non-retryable failure, explicit-port and other-family forwarding, and preservation of caller address bytes are covered. The fixture opens no sockets.

**Status:** confirmed inherited RootHide boundary defect, scoped to exhausting the high automatic-port range with EADDRINUSE results.

**Evidence:** `BaseBin/launchdhook/src/roothider.m:53-63,68-78` uses `in_port_t` for a loop ending at 65,535. That 16-bit value wraps to zero on increment; its `<= IPPORT_HILASTAUTO` condition cannot terminate the range. The next request is therefore for port zero, outside the explicitly enumerated range. Continued EADDRINUSE results can make the loop repeat; another result can terminate it, so an infinite loop is conditional rather than inevitable.

**Local validation:** compiled the actual `new_bind` against a fake bind operation that reports EADDRINUSE for every high-range port and EIO on the first out-of-range call. Both IPv4 and IPv6 produce `calls=16385 out_of_range_port=0`. No sockets were opened. Use a wider retry counter and verify the exhaustion outcome independently from ordinary successful automatic binding.

### PR-038 - P2 - A trustcache batch crossing a cache boundary duplicates its prefix and drops later hashes

- [x] **Resolution:** insertion now indexes the input by the number of entries already accepted, allocates its 0x4000 scratch buffer once rather than once per cache, stops on failed growth, and preserves metadata/read/write failures instead of continuing through address zero or reporting success. It also rejects impossible or changed lengths before modifying the selected cache. `python3 -m unittest discover -s tests -p test_trustcache_batch.py -v` first reproduced nine failing scenarios, then passed the repaired 12-case suite and an expanded 14-case suite (one unittest, ASan/UBSan, exit 0). The actual packed declarations, sorter and insertion loop operate only on fixture-owned memory. Coverage includes empty/exact/partial/full cache boundaries, a 72-cache operation, refusal of initial/later growth, each read/write boundary, corrupt/changed lengths, unique preservation of every input entry, and unchanged error codes. Earlier successful batches remain inserted when a later operation fails; this fix does not claim whole-batch rollback or real-kernel synchronization.

**Status:** confirmed defect present in all four source snapshots, not newly introduced by the port. It affects both trust modes because they converge on this insertion routine.

**Evidence:** `BaseBin/libjailbreak/src/trustcache.c:189-222` decrements `remainingEntryCount` after filling a cache but never advances the input pointer or input index. Each subsequent cache receives `entries[i]` starting at zero again (`215`). Therefore crossing even one boundary can omit part of a small request, not only a request larger than one whole cache.

**Local validation:** the actual insertion function, real 0x4000 cache size and packed entry layout were compiled with mocked memory access and cache enumeration. With 739 of 740 slots already occupied, submitting hashes A and B reports `result=0 cache_count=2 first_hash_copies=2 second_hash_copies=0`. No live trustcache was accessed. The fixture uses the real source loop and is clean under ASan/UBSan.

**Consequence:** a successful recursive/single-file trust RPC can leave the second requested slice or dependency untrusted when a batch happens to straddle an existing cache boundary. A later retry against the newly available cache may behave differently, which can resemble a loader-hook or timing problem. This is a predicted consequence of the reproduced insertion result, not attribution of a previous device failure. Test empty, partially filled and exactly full caches, and retain an input cursor across every batch.

**Related unchecked failure:** `_jb_trustcache_grow` returns zero on allocation refusal (`169-171`), but this insertion loop proceeds to read/write through the zero result rather than returning failure. Thus fixing only outer callers' ignored return values (PR-010) is insufficient; the insertion layer must first produce a meaningful failure result. The repeated alloca inside the loop is also proportional to the number of caches touched and is not released between iterations.

### PR-039 - P2 - Repeated RootHide queries leak their request dictionaries

- [x] **Resolution:** all seven affected C query/setter paths now release their caller-owned request immediately after the synchronous send, including the once-only mode getter. The shared sender's ownership contract and the already-balanced recursive wrappers remain unchanged. `python3 -m unittest discover -s tests -p test_roothide_request_ownership.py -v` first reproduced 97 unreleased requests in each of the success, transport-failure and server-failure modes, then passed all three modes after the fix (one unittest, ASan/UBSan, exit 0). The actual sender and seven actual client functions execute against reference-counted XPC observation objects; all 291 request lifetimes end, replies are released, no released object is accessed, return values remain correct, and the cached getter sends only once. No RPC reaches a device or service.

**Status:** confirmed inherited RootHide ownership defect, including ordinary successful requests. These functions are C, so the ARC counter-evidence for the Objective-C reply hook in section 5 does not apply.

**Evidence:** `BaseBin/libjailbreak/src/jbclient_roothide.c:57-145` creates request dictionaries for jailbroken, palera1n and blacklist queries but releases only the reply. The mode setter at `252-263` does likewise. `jbclient_xpc.c:74-90` only releases a request it created itself when passed NULL; it does not consume caller-supplied dictionaries. Repeated PID classification from the preference/LaunchServices hooks therefore accumulates requests in long-lived processes. The mode getter has the same omission once per process (`232-249`).

**Local validation:** compiled the actual shared sender and six query/setter functions with reference-counted XPC stubs. Six calls leave six unreleased requests with a successful transport and also with a failing transport. The fixture releases its observation objects afterward and is clean under ASan/UBSan. The recursive trust wrappers correctly release their own arguments (`169-170,222-223`); this is not a claim that the entire XPC client layer leaks every request.

### PR-040 - P2 - Check-in completion does not establish that required sandbox capabilities were acquired

- [x] **Resolution:** check-in now fails before publishing success unless the server produced exactly three nonempty sandbox-extension tokens that fit the fixed Mach reply field. Both dyldhook and systemhook validate the same three-token framing and require every `sandbox_extension_consume` call to succeed; dyldhook only publishes `DYLD_STATE_CHECKED_IN` after that succeeds, while the systemhook fallback returns from initialization on a consume failure. The consume helper deliberately does not call `sandbox_extension_release`: dyldhook is linked self-contained against its empty libSystem and that symbol is unavailable there, so a later-token failure is reported without inventing a rollback dependency that prevents the loader from linking. `PYTHONPATH=tests python3 -m unittest -v test_sandbox_capabilities` passed both focused suites after the revised failure semantics were reproduced red for second/third-token failures (ASan/UBSan fixture compilation, exit 0). `make -C BaseBin/dyldhook -j6 all` then rebuilt all arm64/arm64e iOS 15, 16-17 and 18+ variants and each self-contained-symbol check passed. These tests use fake extension strings/consumers and do not issue or consume real sandbox extensions on iOS; target capability policy remains runtime qualification rather than a source-level success-reporting defect.

**Status:** confirmed cross-layer failure-propagation gap. The actual impact requires a sandboxed process and an issuance, truncation or consumption failure; an already-unsandboxed process need not require these extensions.

**Evidence:** `roothider/common.m:424-447` returns NULL unless all three required extensions were issued. `jbdomain_systemwide.c:244-248` logs that failure and continues rather than failing check-in. The Mach response is zero-initialized and conditionally copies extensions, so NULL becomes an empty string, not a NULL-pointer crash (`jbserver_mach.c:64-86`). Nonempty strings are copied into a 2,000-byte field without checking truncation. At the receiver, `dyldhook/src/main.c:34-49` ignores each consume result and `51-70` marks the state checked-in regardless. The stock-dyld systemhook fallback similarly proceeds after unchecked consumption (`systemhook/src/main.c:366-390`).

**Local validation:** the actual merged-dyld check-in and consume functions were compiled against a fake successful RPC and capability consumer. An empty extension string produces `state=1 consume_attempts=0 accepted_tokens=0`; three failed consumes produce `state=1 consume_attempts=3 accepted_tokens=0`. The successful control accepts all three. The fixture uses no real sandbox tokens or Mach messages and is clean under ASan/UBSan.

**Consequence/adaptation:** later path access or library loading can fail even though both dyld and systemhook accept the completed check-in marker. Define which capabilities are mandatory for each process class and preserve their issuance/transport/consumption outcome before publishing initialization success. A successful RPC transport and a populated root path are weaker guarantees than usable bootstrap access.

### PR-041 - P2 - Privileged jbctl launch still uses lexical version ordering and can lose its credential-drop barrier

- [x] **Resolution:** the existing parent launcher repair uses the app-version comparator, checks allocation/setup/spawn results and positive PIDs, closes unused inherited pipe ends, suppresses SIGPIPE, retries EINTR and preserves release/child-exit errors. Both release operations occur after leaving temporary privilege scopes. The existing receiver validates the descriptor and one-byte marker, rejects EOF/read errors, retries EINTR, removes private arguments and rejects a missing action. Fresh command-tool continuation verification: `python3 -m unittest discover -s tests -p test_jbctl_barrier.py -v` passed all 14 actual-source receiver cases; `python3 -m unittest discover -s tests -p test_jbctl_launch.py -v` passed all 25 actual-source parent scenarios (one unittest each, ASan/UBSan with fatal diagnostics, both exit 0). The receiver's previously unverified missing-action guard is covered. Historical red runs reproduced the original parent failures and 11 of the initial 13 receiver cases. No additional production source repair was needed here. All process, privilege, descriptor and signal operations are fixture observations; no real helper is spawned or signalled and no identity changes. This closes the source-level version/barrier/result contract, not section 6's separate target credential-lifetime or complete activation qualification.

**Status:** confirmed upstream-3 version-gate defect retained in the port. The currently pinned 3.0.9.0 value takes the intended modern path; the failure is a release/migration trap, not a claim that an unavailable newer release was tested.

**Evidence:** `Application/Dopamine/Jailbreak/DOEnvironmentManager.m:380-383` selects the legacy launch method with strcmp against 3.0.5, independently of the corrected app-version comparator. A version string 3.0.10.0 compares lower and incorrectly selects the old method. That method resumes jbctl inside the root/unsandbox blocks (`419-425`), whereas the --waitfor pipe deliberately delays it until after those blocks have ended (`392-407,435-439`). The source identifies leaving the temporary privileged state before execution as necessary on iOS 17+.

**Local validation:** the actual condition selects legacy for 3.0.4 and 3.0.10.0, and modern for 3.0.5 and 3.0.9.0. No process was spawned. This proves the incorrect gate; it does not reproduce the possible credential-lifetime panic. A failed legacy spawn also reaches kill with the initial PID zero (`413-422`), and both branches eventually wait without first requiring a successful valid child (`446`). Check spawn/pipe outcomes as well as version ordering rather than relying on this helper's return value as a reliable launch result.

### PR-042 - P2 - XPF teardown omits the monitor resources added for Dopamine 3

- [x] **Resolution:** the existing teardown repair frees all four monitor sections, both monitor containers and both decompressed monitor buffers, in dependent-before-backing-storage order. It also frees eight previously omitted kernel sections, releases kernel backing buffers after section/container disposal, rejects `MAP_FAILED` during unmapping, and initializes/resets the unowned kernel descriptor to -1 so repeated stop cannot close stdin. The historical pre-fix fixture reproduced leaks/order violations and repeated descriptor closure; its older blocked post-fix command was not recovered. Fresh continuation verification: `python3 -m unittest discover -s tests -p test_xpf_teardown.py -v` passed the actual-state/initializer/teardown fixture across 283 full/partial/repeated-stop cases (one unittest, ASan/UBSan with fatal sanitizer diagnostics, exit 0). Resource releases and descriptor operations are tracked local observations, including dependent-before-owner order and owned descriptor zero. No firmware is loaded and no real descriptor or kernel memory is accessed. This closes the resource-lifetime omission, not offset-finding correctness or device support.

**Status:** confirmed upstream-3 resource-lifetime omission retained by the port; scoped to a patchfinder run supplied with SPTM/TXM images.

**Evidence:** `BaseBin/XPF/src/xpf.c:635-661` creates independent decompressed SPTM/TXM buffers, Mach-O containers and text/string sections. `xpf_stop:839-882` releases the kernel resources but none of these monitor resources, then clears gXPF and loses the owning pointers. A later patchfinder run or a failed partial initialization cannot recover those allocations from the cleared state.

**Historical pre-fix validation:** compiled the baseline XPF structure and teardown with tracked allocation/free stubs. One kernel-container control plus eight monitor objects produced `allocated=9 released=1 unreleased_monitor_objects=8 monitor_pointers_erased=1`. The observation harness subsequently freed its retained references; no firmware was loaded and no ASan/UBSan diagnostic occurred in that run. This reproduced the omitted ownership cleanup; the fresh expanded passing fixture above verifies the repair. Neither result measures memory retained for a particular device image. Do not infer that runs without monitor inputs leak these eight objects.

**Counter-evidence:** xpf_stop does not free gXPFError (`818-837`), so the update path's immediate use of the error string after stopping is not the suspected use-after-free. Monitor cleanup and error-string lifetime are separate contracts.

### PR-043 - P2 - Fork repair can be bypassed by failed handshake I/O or hang after a peer exits

- [x] **Resolution:** the current tree already contains the complete handshake repair and it was freshly verified in this continuation. Each fork branch closes the two pipe ends it never uses before entering the protocol, so peer exit can surface as EOF instead of being masked by a locally retained writer. Readiness and acknowledgement are exactly one-byte protocol operations with EINTR retry, poll-bounded waiting, EOF/short/error rejection and a five-second terminal timeout. Parent-side readiness, repair and acknowledgement failures terminate the child and abort rather than letting it run with incomplete repair; child-side readiness/acknowledgement failures abort rather than returning from fork as repaired. `PYTHONPATH=tests python3 -m unittest -v test_fork_handshake` freshly passed the actual-source eight-scenario fixture (one unittest, ASan/UBSan, exit 0), covering child/parent EOF, write errors, EINTR, repair refusal and verification that unused ends are closed in each branch. `make -C BaseBin/forkfix -j4` then completed successfully (exit 0). The fixture performs no real fork, signal, Mach request or kernel mutation. This closes the bounded handshake/I/O defect; target credential/TXM effects after a successful repair remain V-03 rather than part of this source closure.

**Status:** confirmed inherited handshake defects present in all four snapshots. The port preserves the newer server-side TXM repair but does not strengthen the existing readiness/acknowledgement protocol.

**Evidence:** `BaseBin/forkfix/src/main.c:31-56` ignores both readiness and acknowledgement read/write results. On a failed child read, `forkfix___fork:69-77` closes its pipes and returns to the child without establishing that the parent completed repair. On a failed parent read it proceeds to the repair RPC without establishing child readiness. Both sides retain all four pipe ends until after the handshake (`18-28,61-76`), so the parent retains a writer for its own readiness reader and the child retains a writer for its own acknowledgement reader. Peer exit therefore does not necessarily produce EOF to release a blocked wait. The raw assembly read/write wrappers also return directly after the syscall rather than supplying a retry policy (`forkfix/src/syscall.S`).

**Why this matters for Dopamine 3:** `launchdhook/src/jbserver/jbdomain_systemwide.c:419-512` copies the relevant VM permissions/debug state and, where present, repairs the TXM address space. The child must not treat an interrupted/failed read as evidence those operations completed. Conversely, a failed peer cannot be allowed to leave a live caller waiting on a pipe that it still holds open itself. The checked repair-RPC result in the parent is real, but does not validate either pipe boundary.

**Local validation during this continuation:** compiled the actual `open_pipes`, `close_pipes`, `child_fixup`, `parent_fixup` and `forkfix___fork` from each of the four trees with non-device syscall/RPC stubs. Failed child reads return child PID 0 after one attempt and no completed repair; failed parent reads still trigger the repair call. Tested both conventional -1/EINTR and a positive raw-error return. A separate ordinary nonblocking pipe observation returns EAGAIN while the otherwise-unused local writer is open and EOF after it is closed. All four ASan/UBSan fixtures pass without performing a real fork, signal, Mach request or kernel mutation. Evidence: `review-evidence/checks/fork-handshake.py`, generated per-tree C fixtures and logs.

**Required adaptation/validation:** close each unused end at the appropriate fork branch, require complete readiness/acknowledgement messages, handle interruptions and peer failure, and define a bounded terminal outcome. Test child-before-ready exit, parent-before-ack exit, interrupted/short I/O and repair refusal. The globals' interaction with concurrent libc fork calls has not been established here and is not an additional claimed race.

### PR-044 - P1 conditional - Initial launchd injection has an incomplete failure and completion contract

- [x] **Resolution:** the injector/app boundary now has an explicit completion contract. `opainject` propagates task-port, PAC-helper spawn/exit, remote-thread, sandbox-extension, `dlopen` and arbitrary-call failures instead of returning success after an incomplete injection; its arbitrary-call cleanup balances peer suspensions and restores/deallocates owned thread/stack state on every returning failure path. The app checks the stash helper's exit status, owns the primitive-transfer receiver until it has been cancelled and joined, and requires the handoff semaphore within a 60-second deadline rather than treating injector exit as completed launchd recovery. Fresh `PYTHONPATH=tests python3 -m unittest -v test_launchd_injection_completion` passed both focused current-source contract tests. A fresh `gmake -C BaseBin/opainject -j4` under the repository RootHide-Theos/tool environment compiled, linked, merged and signed both arm64 and arm64e variants successfully. This closes the source-level false-success, suspension-leak, worker-lifetime and unbounded-handoff defects; later launchd re-exec/runtime recovery remains covered separately by the lifecycle validation matrix.

**Status:** confirmed inherited defects at the injector/app boundary and in injector suspension cleanup. The pinned opainject sources are identical in all four uploaded trees; this is not a newly introduced RootHide algorithm regression.

**False success:** `BaseBin/opainject/main.m:41-92` returns void from the PAC helper launcher; task-port refusal, helper spawn failure and a nonzero child exit are not propagated. Its arm64e entry path returns 0 immediately afterward (`105-115`). The direct path also calls the void `injectDylibViaRop` and returns 0 (`152-156`); that helper can return before loading anything when remote-thread creation fails (`rop_inject.m:424-426`) and merely prints a failed dlopen (`444-456`). `DOJailbreaker.m:448-455` trusts that process exit status and then waits forever for the primitive-handoff semaphore. A successful injector exit therefore does not establish that a receiver exists.

**Suspension cleanup:** `opainject/rop_inject.m:293-328` suspends the other target threads before the remote call. If `wait_for_thread` fails, it returns at 313 without the peer-resume operation at 328. When the target is launchd, this can retain suspensions on system-critical threads even though the top-level injector reports success. This is distinct from PR-024's failed executable-page restoration in hookd.

**App-side ownership:** the app starts a detached server thread using a pointer to stack-local `boomerang_info` (`DOJailbreaker.m:420-426`). Early returns on stash spawn/wait or injector failure do not stop/join that worker or keep its context alive (`435-451`). An error response must not leave a receiver referring to an expired stack frame. The stash helper's actual exit status is also never required to be successful after waitpid (`440-445`).

**Local validation during this continuation:** the actual injector main/PAC launcher/ROP entry logic was compiled as a non-device C fixture, replacing only the autorelease-pool scope and external process/Mach operations. Task-port refusal, helper spawn refusal, helper exit 42 and remote-thread creation refusal each yield CLI result 0. A second fixture compiles the actual `arbCall`: the successful control balances the peer suspension, while an injected wait error returns 5 with one peer suspension still held and zero peer-resume calls. These fixtures do not inject code, spawn a real helper or touch any target process. Observation-only allocation cleanup keeps the final ASan/UBSan runs clean. Evidence: `review-evidence/checks/injector-outcome.py`, `injector-suspension.py`, generated C and logs. The detached app-worker lifetime is a source finding, not a reproduced real worker use-after-return.

**Required adaptation/validation:** explicit injector outcome, checked stash outcome, a bounded completion handshake, cleanup of every acquired suspension, and a worker context that outlives the worker on every return path. Validate startup refusal and partial injection separately from successful primitive recovery and later userspace re-exec.

### PR-045 - P2 - Watchdog recovery delays package-daemon startup until a modal alert is dismissed

- [x] **Resolution:** required package-daemon bootstrap and optional application refresh now finish before the panic query/notification. The existing RootHide launchctl path/domain and uicache opt-out are preserved. Startup returns the first required command failure, or zero after successful required work; failed/absent diagnostic display does not falsify that outcome. The diagnostic alert has a 30-second timeout instead of retaining the startup job indefinitely. `python3 -m unittest discover -s tests -p test_service_startup.py -v` first reproduced 48 failed assertions across ten actual-source cases, then passed all ten after the repair (one unittest, Foundation/ASan/UBSan, exit 0). The fixture covers normal/panic startup, opt-out, absent panic text, alert refusal, launchctl refusal and uicache refusal while observing every command/query/alert boundary. No service or notification was actually launched. Required-operation failure is explicit; the helper deliberately does not automatically replay a possibly partially successful directory bootstrap. This closes the ordering/result defect, not the separate target recovery matrix.

**Status:** confirmed inherited RootHide lifecycle coupling. The condition is a reported userspace panic followed by the protected restart path, not an ordinary startup with no panic message.

**Evidence:** RootHide removes automatic enumeration/loading of the bootstrap's `/Library/LaunchDaemons` from `BaseBin/launchdhook/src/daemon_hook.m:45-59`, leaving basebin jobs there and delegating package jobs to its path-aware launchctl. The replacement call is in `jbctl/src/internal.m:192`. However, `startup` first invokes `CFUserNotificationDisplayAlert` with timeout 0 at `181-185`. Apple's [CFUserNotificationDisplayAlert documentation](https://developer.apple.com/documentation/corefoundation/cfusernotificationdisplayalert%28_%3A_%3A_%3A_%3A_%3A_%3A_%3A_%3A_%3A_%3A_%3A%29) explicitly specifies a blocking call and no timeout when zero is passed. Therefore the new daemon-loading boundary is downstream of user interaction. Upstream Dopamine 3 also shows an alert, but its launchd-side package-daemon registration has not been removed; the combined dependency is introduced by the RootHide scheduling change and retained in the current port.

**Consequence:** after watchdog recovery, package services may remain unavailable until the alert is dismissed even though launchd and the interface appear to have restarted. This does not prove that SpringBoard itself must deadlock or that a particular previous SSH observation took this path. An alert creation failure may return immediately; the problematic path is a successfully displayed, unanswered alert.

**Related outcome gap:** the subsequent launchctl bootstrap and uicache results are ignored (`192,202`). Normal startup then falls through to return -1 (`213`), except for the explicit disabled-uicache shortcut. The RunAtLoad startup plist sets KeepAlive false and provides no retry policy for a failed bootstrap. These facts make startup completion and its exit code inadequate indicators that the intended package services were registered.

**Validation/adaptation:** this finding follows the actual four-way source ordering and Apple's independently checked API contract; no modal dialog or daemon job was launched during the review. Later tests should delay/dismiss/refuse the alert independently of service bootstrap, inject launchctl failure, and distinguish package-daemon registration from merely starting the basebin startup job. Required infrastructure should not depend on acknowledging a diagnostic message.

### PR-046 - P2 - Static trustcache replacement destroys the previous cache before allocating its replacement

- [x] **Resolution:** different-sized static-cache replacement is now prepared before the prior allocation is retired, and publication failure rolls back to the previous list entry. The function allocates the replacement before removal, retains the old allocation after unlinking it, populates the new allocation, checks the new list insertion, and on insertion refusal reinserts the old cache and frees only the unpublished replacement. Only after successful publication does it apply the existing reader grace period and free the retired allocation. The focused fixture was expanded first and reproduced the remaining false-success/data-loss path (`test_trustcache_replacement` failed with fixture exit 5 when insertion was refused), then passed after repair. Fresh combined verification `PYTHONPATH=tests python3 -m unittest -v test_trustcache_batch test_trustcache_replacement` passed both unittests under ASan/UBSan, covering allocation refusal, insertion refusal with rollback, successful different-size retirement and the existing batch/cursor error cases. `make -C BaseBin/libjailbreak -j4` rebuilt the arm64/arm64e library and lipo output successfully (exit 0); existing qualifier/pointer warnings in unrelated RootHide source remain. No kernel list or allocation is touched by the fixture. This closes the source-level remove/free-before-replacement and publication-failure ordering defect; real kernel reader synchronization remains a separate validation obligation.

**Status:** confirmed inherited failure-ordering defect in all four snapshots, reproduced using their actual upload functions with non-device allocation/list/memory stubs. This is distinct from PR-038's dynamic batch cursor and PR-015's filesystem publication.

**Evidence:** `BaseBin/libjailbreak/src/trustcache.c:352-375` compares the old and new allocation sizes. When they differ, it removes the existing UUID-matched cache, sleeps, frees its allocation, and only then attempts to allocate the replacement. Allocation refusal returns -1 without reinstating the previous cache. The equal-size branch does not take this removal path. RootHide's basebin loader uploads under a stable BASEBIN UUID (`roothider/common.m:559-599`), and the live updater invokes it before publishing the replacement files (`launchdhook/src/update.m:32-61`). A basebin update whose collected hash count changes can therefore exercise this transition.

**Consequence:** an update can correctly report failure yet leave neither the previous static trustcache nor its replacement published. Returning an error is not rollback. The resulting effect on already mapped images, later executable-page validation and future launches requires target testing; no particular device failure is attributed to this path. The code's fixed sleep is not independently established here as a safe grace period for concurrent kernel readers.

**Local validation:** all four actual upload functions report `result=-1 old_present=0 new_present=0 old_freed=1 alloc_calls=1` when a differently sized replacement allocation is refused. Current/upstream-3 same-size and successful different-size controls retain the old cache or publish the new cache respectively. Failure/control cases are clean under ASan/UBSan. The old upstream-2 and legacy-RootHide same-size controls exposed a separate historical overread already corrected by upstream 3, recorded in section 5 rather than treated as a current bug. Evidence: `review-evidence/checks/supplementary-failures.py` and its per-tree trustcache fixtures/logs. All addresses and list operations belong to the fixture's ordinary memory model; no kernel trustcache was read or written.

**Required adaptation/validation:** prepare the replacement before retiring the prior state, define publication and reader-lifetime ordering, and test same-size/different-size replacements, allocation refusal, copy failure and list-publication failure separately. A successful source-level allocation refusal test will not validate real kernel reader synchronization.

### PR-047 - P1 conditional - The process restart helper advertises an argument buffer larger than its allocation

- [x] **Resolution:** the existing source repair allocates the advertised argument capacity once, checks both allocations and query extents, rejects nonpositive PIDs, and requires the executable path to terminate inside the returned extent. Invalid ARGMAX results use the existing 4,096-byte fallback; the unsynchronized static cache is removed. Fresh verification on 2026-09-15: `python3 -m unittest discover -s tests -p test_restart_process.py -v` passed the original eight scenarios, then passed the expanded 20-scenario fixture (one unittest, ASan/UBSan with fatal diagnostics, exit 0). Added coverage includes NULL paths, empty/refused/oversized process-table responses, negative PIDs, unmatched/short paths, refused/negative/truncated ARGMAX results and buffer reuse across two processes. Every scenario now verifies that both owned allocations are released exactly once, and every argument query observes the full allocated capacity. The earlier blocked result was not recovered; these are new completed runs against the current actual function. All sysctl/signal operations are observations: no real process is queried, restarted or signalled. This closes the allocation/bounded-parsing defect, not device restart qualification.

**Status:** confirmed inherited capacity mismatch in all four snapshots, with the resulting heap overwrite reproduced under ASan using simulated argument-query output. Target impact is conditional on returned process arguments exceeding the separately sized allocation; this is not a demonstrated launchd crash on the phone.

**Evidence:** `BaseBin/libjailbreak/src/util.c:1265-1286` first obtains `length` for the KERN_PROC_ALL process table. Inside the per-process loop, it sets `size = maxArgumentSize` but allocates `malloc(length)`, then passes that allocation and `&size` to KERN_PROCARGS2. These sizes describe different objects. A process-table byte count below the allowed argument size therefore produces an overstated output capacity. The path also lacks an allocation check for this inner buffer. This helper is active: `launchdhook/src/main.m:70` and `jbctl/src/main.m:165` use it for backboardd, and `launchdhook/src/jbserver/jbdomain_systemwide.c:327` uses it for the keyboard restart after a later SpringBoard check-in.

**Consequence:** the failure need not involve an unusually long command line in the target daemon: the helper queries the arguments of every enumerated nonzero PID before comparing executable paths. A different process with longer arguments can trigger the capacity mismatch while launchd or jbctl is attempting an unrelated restart. Whether a given real process table and argument set satisfy the size condition remains unverified.

**Local validation:** compiled each of the four actual killall functions against simulated sysctl and signal operations. A deliberately modeled two-record process table occupies 128 bytes, while the argument query is supplied a 4,096-byte capacity. Returning a 512-byte argument record triggers the expected ASan heap-buffer-overflow in every tree; query-failure and 64-byte-record controls complete without target signals or sanitizer errors. The fixture's 64-byte process structure is explicitly a model, not a claim about Darwin's actual kinfo_proc size. The invariant is the use of two unrelated sizes, not those particular numbers. Evidence: `review-evidence/checks/supplementary-failures.py`, generated `*-killall-capacity.c`, observation logs and separate expected-failure ASan logs. No real process arguments were queried and no signal was sent.

**Required adaptation/validation:** allocate the capacity actually supplied to the argument query, check allocation/query outcomes, and bound executable-path parsing by the returned extent. Test small process tables with longer argument records, truncated/error results and allocation refusal without sending signals to real processes.

### PR-048 - P2 - Cross-process trust reopens a pathname without preserving the caller's file identity

- [x] **Resolution:** the remote-descriptor path now requires a complete pathname-info reply and a nonempty terminated path, compares the reopened descriptor's device/inode/generation with the reported object, then queries the caller descriptor again before signature processing. A different object produces ESTALE; query/stat failures abort without trust/attachment and close the reopened descriptor. The server-local path remains `dup`, and hardlinks to the same object remain valid. `python3 -m unittest discover -s tests -p test_trust_file_identity.py -v` reproduced nine failures in its initial 15 scenarios, then passed all 15 after repair. Coverage was expanded and the final fresh run passed all 21 scenarios (one unittest, ASan/UBSan, exit 0), including ordinary-file replacement/unlink, malformed/failed queries, identity mismatches, descriptor reuse/recheck failure, hardlinks, local duplication, optional attachment and supplied file/process/allocation signature sources. Real temporary-file descriptors and rename/unlink operations are used; caller-query responses and signature/attachment operations are observations. No real process is queried or modified. The repair detects object substitution rather than introducing a new descriptor-transfer protocol, and it does not claim to freeze in-place writes or validate the separate signature/content-consistency contract.

**Status:** confirmed pathname-to-file identity gap, inherited from the earlier trust service and retained in Dopamine 3/current port. The actual-source reproduction covers the newer trust-and-attachment interface; the older sources independently contain the same pathname-reopening step.

**Evidence:** `BaseBin/launchdhook/src/jbserver/jbdomain_systemwide.c:99-118` receives a descriptor number belonging to the requesting process. For a non-NULL process token, it queries that descriptor's pathname with proc_pidfdinfo and opens the pathname again (`112-114`). It never establishes that the new descriptor references the same file object. Later signature collection/trust and optional F_ADDSIGS attachment use this newly opened descriptor (`144-168`). The no-token, server-local path correctly uses dup (`105-108`); this finding is not about that branch. The older reopening steps are at upstream-2 `jbdomain_systemwide.c:150-152` and legacy-RootHide `157-159`.

**Consequence:** a pathname replacement between the query and reopen can leave the caller holding the old object while the server prepares the new object. The [POSIX rename contract](https://pubs.opengroup.org/onlinepubs/9799919799/functions/rename.html) preserves an open file's contents until its references are closed even after its last name is removed. This outside reference establishes the pathname/open-reference distinction; the actual selection mismatch is demonstrated by the local fixture. Atomic package replacement is a relevant scenario, not an established cause of any previous PAM failure. A valid same-path string is not a file-identity guarantee, and Dopamine 3's optional server-side attachment makes the distinction consequential beyond hash collection.

**Fresh local validation:** reran `review-evidence-final/checks/trust-fd-identity.py` against the actual upstream-3 and current-port functions. Ordinary temporary files and an actual rename exercise the query/reopen interval; signature processing and attachment are observation stubs. Both trees return `result=0 trusted_original=1 attached_original=1` without replacement and `result=0 trusted_original=0 attached_original=0` with replacement. Both make one attachment call. ASan/UBSan report no fixture errors. This proves which file object reaches those operations, not whether the real kernel would accept the signature or whether a real target hits that timing.

**Required adaptation/validation:** preserve the intended file object across the request, or detect a changed object and return an explicit failure before trust/attachment. Validate replacement, unlink, same-path reopen, caller descriptor reuse, remote versus server-local requests, and file/process/allocated signature sources. File identity alone also does not freeze in-place content changes; the signature/contents consistency contract remains necessary.

### PR-049 - P2 - The signature hook returns Mach transport failures through the fcntl success-value domain

- [x] **Resolution:** completed normalized-attachment failures pass through an explicit Mach/server-status-to-errno conversion and dyld's existing `cerror` boundary, returning -1 rather than a positive Mach value. Known resource/protection/argument/timeout/dead-port errors are mapped explicitly; negative server and unknown failures become EIO while retaining the original status for error-only diagnostics. Success and the initial kernel-success/skipped-normalization paths remain unchanged. The historical fixture reproduced 96 failed assertions across 55 actual-hook cases. Fresh current-source verification after the PR-034 extent changes passes `PYTHONPATH=tests python3 -m unittest -v test_fcntl_outputs test_fcntl_status`, and `make -C BaseBin/dyldhook -j4` rebuilds and validates all six target variants (exit 0). This closes the libc error-domain defect against the current hook rather than relying on the earlier pre-PR-034 run.

**Earlier implementation and verification:** completed normalized-attachment failures now pass through an explicit Mach/server-status-to-errno conversion and dyld's existing `cerror` boundary, returning -1 rather than a positive Mach value. Known resource/protection/argument/timeout/dead-port errors are mapped explicitly; negative server and unknown failures become EIO, with their original status retained in an error-only diagnostic. Success and the initial kernel-success/skipped-normalization paths remain unchanged. `python3 -m unittest discover -s tests -p test_fcntl_status.py -v` reproduced 96 failed assertions across 55 actual-hook cases, then passed all 55 (ASan/UBSan, exit 0). Coverage includes all four accepted commands, positive Mach errors, negative server results, unknown status, successful attachment and fast paths, without Mach traffic or real attachment. `make -C BaseBin/dyldhook -j4` then rebuilt and signed all six arm64/arm64e iOS15/16-17/18+ variants and passed every self-contained-symbol check (exit 0). An existing `spinlock_fix.c` unknown-`optimize`-attribute warning remains unrelated to this fix. PR-034's command-specific output/extent issue remains separate and unchecked.

**Status:** confirmed upstream-3 error-domain mismatch retained by the current port, freshly reproduced with the actual hook. It is distinct from PR-034's missing INFO output fields after successful attachment.

**Evidence:** in the TXM normalization branch, `BaseBin/dyldhook/src/lv_bypass.c:259-281` assigns the result of jbclient_mach_trust_file to r, marks the operation finished, and returns r directly. The client returns a nonzero Mach transport status without conversion (`libjailbreak/src/jbclient_mach.c:133-136`); for example, the local port-acquisition checks can return KERN_FAILURE (`30-38`). By contrast, the hook's ordinary syscall path uses msyscall_errno, whose failure branch enters cerror (`dyldhook/src/reimpl.c:34-47`). This is therefore a libc-style return boundary, not a raw positive-error syscall interface. Apple's [fcntl manual](https://developer.apple.com/library/archive/documentation/System/Conceptual/ManPages_iPhoneOS/man2/fcntl.2.html) specifies -1 with errno on failure.

**Consequence:** a positive transport failure can be interpreted as success by callers testing for -1 or a negative result, while errno does not describe that failed request. Callers explicitly requiring zero may instead reject it; their diagnostic still needs the correct error domain. The current port reaches this path after its initial kernel operation fails, only for checked-in processes and signatures requiring normalization. The upstream-3 hook lacks that preliminary kernel attempt. Ordinary successful kernel handling in the current port returns early and is not affected.

**Fresh local validation:** reran `review-evidence-final/checks/fcntl-error-domain.py`, compiling each actual hook with non-device signature/Mach/transport stubs and ASan/UBSan. RPC results 5 and 268451843 are returned unchanged as fcntl results by both trees. The current hook makes one initial raw-call attempt and one attachment request; errno remains the fixture's preceding EACCES value, 13. Upstream 3 makes no preliminary raw call and leaves errno at 0. The successful RPC control returns 0. Stale errno after that successful control is not itself a bug: errno is only meaningful when failure is reported. These observations do not establish how every real dyld caller interprets the result, and no Mach traffic or signature attachment was performed.

**Required adaptation/validation:** map transport and server failures explicitly to the intercepted function's -1/errno convention while retaining useful underlying status for diagnostics. Cover positive Mach failures, negative server results, successful normalization, the initial kernel-success fast path, and all accepted signature commands. Do not fix this by assuming every nonzero status is already an errno value.

## 5. Already corrected defects and counter-evidence

- **Static-trustcache payload overread was fixed upstream and is correctly retained:** the upstream-2 and legacy-RootHide upload functions pass the header-plus-payload allocation size when copying from a payload-only buffer. Upstream 3 and the current port subtract `ksizeof(trustcache)` from the copy extent (`trustcache.c:357,383` in the current tree). The supplementary same-size fixture reproduces the historical 86-byte read from a 46-byte payload with a modeled 40-byte kernel header; current/upstream-3 controls are sanitizer-clean. This is positive evidence against restoring the older upload implementation wholesale. It does not fix PR-046's independent remove/free-before-allocation ordering.

- **Mode-toggle cache hypothesis narrowed:** `Application/Dopamine/UI/Settings/DOSettingsController.m:737-745` applies the running-mode setter only in the explicit reboot-now action, saves the preference, and requests a userspace reboot. Therefore a process-local cached mode is not, by itself, proof that the normal UI promises live mode convergence without a restart. Setter/reboot failure and direct API callers still need their own handling; the initial broad claim was not promoted.

- **Early hook-publication race not promoted solely from initializer order:** launchdhook installs some hooks before completing its RootHide post-initialization, but the actual first-load injector suspends the other enumerated target threads around the remote dlopen (`opainject/rop_inject.m:293-328`). A claim that all existing launchd threads necessarily observe that initialization window would ignore this coordination. New-thread creation and suspension failures still need their own analysis; PR-044 records the independently established failure cleanup rather than assuming unrestricted concurrency.

- **XPC reply lifetime candidate not promoted:** `roothider/xpc_hook.m:96-102` obtains the saved request and then removes its dictionary entry. A C/MRC reading suggests a borrowed-reference use-after-free, but this `.m` file is built with `-fobjc-arc` (`BaseBin/libjailbreak/Makefile:12`), and the local XPC object is strong. The existing ownership fixture also preserves the SDK's create-reply return annotation. A generic missing-retain claim would ignore this counter-evidence.
- **Private bootstrap dispatch adapted:** `launchdhook/src/roothider.m:96-99,182` passes the global-server dispatcher to `initJailbreakd`; `libjailbreak/src/roothider/jailbreakd.c:117-122` explicitly dispatches and releases a successfully received request. The former assumption that an inline libxpc hook consumed that request is no longer present in current source.
- **RPC domain collision avoided:** upstream Dopamine 3's app domain is moved to 6, and legacy roothide remains 5 in `libjailbreak/src/jbserver_domains.h`; `launchdhook/src/jbserver/jbserver_global.c` matches that ordering. Renumbering legacy roothide to accommodate upstream would break existing clients; the current arrangement does not make that mistake.
- **Serialization additions are present:** the roothide brand, palera1n flag, dyld-mode flag and extra kernel symbols are included in the relevant `info.h` iteration macros. The upstream 3 kernel/primitives/translation implementations are retained. This is not a port that silently reverted those entire subsystems to Dopamine 2.
- **Hot-update offset loss is already addressed in the initial dirty tree:** `launchdhook/src/update.m:124-185` uses supported upstream XPF sets, starts from `jbinfo_get_serialized()` and overlays newly discovered values. That retains boot-resolved roothide fields on the same kernel instead of discarding them or requesting absent legacy sets. This change was uncommitted before the review and remains so; it must not be reported as missing or erased during later fixes.
- **Ordinary on-disk branding and signature ownership fixes have positive evidence:** the actual current `signatures.c`, `recdhash.m` and `code_signing.c` pass host fixtures using the bundled killall, dash and pam_unix.so. The first code-page hash matches the branded bytes; repeated trust is stable; ordinary branding does not spuriously request detached attachment; synthetic TXM normalization requests owned attachment bytes when required. The fixture mocks the trustcache and attach operations, so this proves code-path/byte ownership behavior, not kernel acceptance or successful PAM authentication. PR-007 concerns a different, recursive path.
- **launchctl compatibility is adapted, not simply replaced:** `Packages/launchctl/compat.py` preserves the roothide path changes while making the removed user-switch import optional and replacing that unsupported operation with ENOTSUP. Bootstrap finalization installs the compatible package before setup scripts. Eight launchctl regression checks, including package/Mach-O checks, pass. A generic recommendation to replace it with stock upstream launchctl would lose required roothide behavior.
- **Relative dependencies and fallback-provider exports are verified:** the selected linkage tests confirm that the auxiliary hook library can resolve libroothide without waiting for a lazy `.jbroot` link, and that the fallback provider/archive exposes the expected Dopamine 3 adapter. These are linkage/package checks, not runtime execution of the adapter. Its subsequently completed source review produced the independent findings in PR-029/030.
- **Boomerang and crash-reporting adaptations have targeted checks:** three compiler-level boomerang tests verify fresh control-port acquisition, one resume and failure ordering; three crash-reporter checks preserve older behavior while guarding the incompatible path on iOS 17 and later. They do not exercise the separate `exec_trace.m` exception backend or establish device-level reboot recovery.
- **App version formatting and Debian package ordering are different domains:** all ten existing app-version checks pass. The three new Debian counterexamples in PR-011 remain valid; passing numeric app-version tests does not justify reusing that comparator for Debian packages.
- **PID reuse is not uniformly ignored:** the blacklist cache carries process-generation information, so a generic claim that all blacklist entries are keyed only by reusable PIDs would be wrong. Exec-trace/event records and requests still need their own lifetime analysis rather than borrowing that guarantee.
- **The exception-count hypothesis was actively disproved:** the actual host query and current MIG definition contradicted the old manual/kernel-helper reading; PR-020 records the correction. Only the independently established return-value, cleanup and restore defects remain findings.

## 6. Open hypotheses and validation gaps

These are explicit remaining obligations, not additional confirmed causes of a boot, SSH or PAM failure. In particular, previous device trials and saved notes were used to choose what to inspect, not to prove that every present source defect caused the latest symptom.

Each checkbox below corresponds to the same-ID row in the validation matrix. It remains open until that row's evidence requirement is met; a source-level finding's resolution does not close it automatically.

- [ ] **V-01:** current loader dispatch and PAC.
- [ ] **V-02:** executable-page transition and remote dyld.
- [ ] **V-03:** credentials and process identity.
- [ ] **V-04:** modern exception tracing.
- [ ] **V-05:** exact kernel layouts.
- [ ] **V-06:** recursive trust equivalence.
- [ ] **V-07:** interrupted bootstrap and update.
- [ ] **V-08:** installed binary and package ABI.
- [ ] **V-09:** app/preferences/UTType hiding.
- [ ] **V-10:** special routes and degraded modes.

| Gap | Exact question to resolve | Evidence required |
| --- | --- | --- |
| V-01: current loader dispatch and PAC | Do every intended public/internal lookup and load entry point preserve the actual caller, namespace, return-address/PAC convention and recursion behavior? Is an old saved function pointer already rebound? | The uploaded current-source pass is complete and PR-025 records installation failures. Still required: actual dyld-variant/architecture ABI verification, both global modes and per-process exceptions, internal as well as public entry points, failed and successful late loads. The uncommitted dispatch/vtable experiments are not runtime-certified. |
| V-02: executable-page transition and remote dyld | Does each patch path use a valid write/execute transition on its exact kernel, and restore all affected pages and suspension state on failure? Is the independently retained remote backend covered? | The uploaded hookd/external-adapter source pass is complete (PR-024/026/029/030). Still required: actual executable-page/cache/PAC behavior, failure recovery and PR-021's independent metadata/page-boundary tests. A provider export or mocked operation ordering is insufficient. |
| V-03: credentials and process identity | Are real/effective/saved IDs, supplementary groups, audit/security tokens, ucred lifetime and shared-credential ownership consistent after helper success/failure, setuid/setgid, exec and fork? | The uploaded copy/helper/check-in/sandbox-extension source pass is complete (PR-031/032/035/040). Still required: actual resulting IDs/tokens/reference state, helper exit/reuse and late reply/cancellation. Separate persona restrictions from credential ownership and loader failures during authentication. |
| V-04: modern exception tracing | Which current targets permit the required task/exception operations, and what happens on every refusal? | Tests for the independent exec backend, including PR-020; real target eligibility is not established by the host query or the older crash-reporter guard. |
| V-05: exact kernel layouts | Do namecache/vnode fields, dynamic globals, PAC representations and the remote task metadata refer to the intended objects for every supported kernel? | Per-kernel structural checks and safe publication invariants. The latest open-source XNU layout is supporting context, not the exact binary layout of each target firmware. |
| V-06: recursive trust equivalence | Do recursion and actual dyld loading agree on architecture, rpath inheritance, weak dependencies, file identity, disk mutation, TXM normalization, attachment and error conventions? | A mixed-slice dependency graph and both modes; mandatory versus weak failures; upload/attachment refusal; existing loaded mappings; replacement between descriptor query and reopening; transport failures through each syscall adapter. PR-048/049 establish source-level mismatches, not real-kernel acceptance or caller behavior. Existing ordinary trust fixtures deliberately do not cover full equivalence. |
| V-07: interrupted bootstrap and update | Can each failed transition return to a usable prior/new state without deleting a valid installation or mixing generations? | Temporary-root fault injection after every rename/link/copy/marker, then later separate cold activation, re-jailbreak, userspace restart and supported same-generation hot update. Do not attempt a userspace-only 2-to-3 primitive handoff. |
| V-08: installed binary and package ABI | Are existing provider/daemon/helper binaries compatible with the new libjailbreak structures and RPC contracts, and are upgrades actually selected? | PR-023 proves the source-layout mismatch using all four declarations. Still required: identify the actual installed historical provider binary, verify its compiled field accesses, and compare clean/incremental archives and migration decisions. Source ABI evidence is not installed-binary provenance. |
| V-09: app/preferences/UTType hiding | Do hooks still match current private method signatures, result mutability, ivars, callbacks and cache behavior? | Per-OS private ABI checks, nested/deferred requests with different classifications, plugin/unit index alignment, reused URL/identifier objects and cache invalidation. See PR-013/014. |
| V-10: special routes and degraded modes | Do palera1n, tweak-disabled/safe-mode provider selection, package removal and custom Wolf identifiers preserve required infrastructure without unintended side effects? | The uploaded provider-transition/daemon-lifecycle source pass is complete. Separate route-specific runtime fixtures remain necessary. Deliberately unsupported standalone/Corellium packaging and stub helpers must not be presented as validated features. |

### 6.1 Order of work after the inventory is accepted

The previously missing loader/hookd/credential source surface has been inspected. The lifecycle pass added PR-043 through PR-045; supplementary cross-path work added PR-046 through PR-049 and a verified upstream correction. Use the lifecycle boundaries in section 3.2 to order later fixes: potentially global stalls and kernel/publication invariants (PR-001/004/005/021/024/033/044/047), trust-mode equivalence, replacement/file identity and error propagation (PR-007/008/009/010/034/038/040/046/048/049), and credential/exec/fork state ownership (PR-020/031/032/035/043). Package selection, binary migration and build-generation checks must precede interpreting another device trial as evidence for the newly built source (PR-011/016/023/041). Recovery service startup, auxiliary app-hiding and special-route tests follow in their own lifecycle/OS matrix rather than being inferred from a successful bootstrap (including PR-045).

This was the inventory's proposed dependency ordering. The subsequent user request authorizes implementation. Explicit call contracts, correct failure returns, bounded waits and targeted reproducible fixtures should precede another broad device-test iteration.

## 7. Coverage and limitations

This document is being updated during the review, not generated only at its end. Source locations refer to the baseline above plus the initial working-tree changes unless stated otherwise. Reference checkouts and generated inventories are ignored audit evidence, not deliverable source changes.

**Access history, no longer a current source-review blocker:** the first remote pass could not complete several combined reads covering loader dispatch, hookd, credentials, sandbox extensions, XPF teardown and ancillary lifecycle code. The user subsequently supplied all four repository ZIPs directly. Those exact uploaded snapshots, including the dirty current-port implementation, are now inspected locally without any Codexify export/materialization operation. PR-022 through PR-042 and their stated local fixtures supersede the old source-access gaps; they do not establish device/private-ABI correctness. The historical installed-provider binary and exact target firmware are not included in the source-only validation claim.

### 7.1 What was actually inspected

All four pinned inputs were compared mechanically, with complete path/diff inventories saved. The semantic pass concentrated on current app activation/bootstrap/removal, launchd startup/re-exec/spawn, IPC transport/domain/argument dispatch, ordinary and recursive signatures, branding, unsandbox publication, exec-event/exception/remote-dyld helpers, shared spawn environment/configuration, blacklist caches, auxiliary hiding hooks, hot update, package preparation and build dependencies. Relevant upstream-to-upstream and fork-to-port changes were checked alongside each path.

The completed source inventory additionally covers the initial injector's return and suspension paths, the parent/child fork handshake, the independent package-daemon startup boundary, fixed Mach-message layouts, native exported client prototypes, actual incremental make prerequisite behavior, cross-process trust file identity and the signature-hook return convention. The earlier mechanical disposition file contains 1,176 unique source-tree paths with four pairwise comparisons and a subsystem label. The report-reconciliation pass independently regenerated the four-way equality/addition/removal/change table and confirmed the same 1,176 paths. Neither table treats a mechanical disposition as semantic certification.

| File/change class | Disposition and limits |
| --- | --- |
| RootHide-specific additions and adapters | Traced through their callers, lifecycle ordering and upstream-3 dependencies; retained defects and new regressions are separated in the numbered entries. |
| Upstream-3 primitives, TXM/SPTM, kernel and client interfaces | Retained implementation was compared against both upstream versions and legacy consumers. Identical source is not treated as proof that old installed clients remain compatible. |
| Legacy `systemhook/src/common.c` and common.h | Relocated into `src/common/`; their absence at the old path is not a missing port. Shared spawn, environment and persona behavior was reviewed at the new location. |
| Legacy exploit/header changes superseded by upstream 3 | The current upstream implementations and eligibility model are retained rather than mechanically reapplying every older fork hunk. Palera1n's distinct transport and the ANE header adaptation were reviewed separately. Individual exploit algorithms were not independently re-proven. |
| Removed fakelib redirect / retained literal rootless strings | Evaluated against the intentionally different branded-root model. A removed bind-mount helper or a /var/jb prefix inside a package-extraction path is not automatically an omission. |
| Existing bundled binaries and private framework interfaces | Package/linkage and prior fixture evidence is retained, but installed-binary provenance, private selector/ABI behavior, executable-page policy and firmware-specific layouts remain the explicit section 6 obligations. No local binary disassembly was substituted for those checks. |
| Build, entitlements, tests, UI and metadata | Relevant integration changes and existing tests were inspected; warm-build prerequisite failure was reproduced. Images/localizations are inventory assets rather than proof of runtime compatibility; full native compilation was not performed in the Linux sandbox. |

This is **not** a claim that every vendor exploit algorithm, localization, third-party implementation or binary has been independently re-proven. Identical-to-upstream files are distinguished from modified interfaces. Source-access gaps have been closed using the uploaded snapshots; the target-ABI, installed-artifact and lifecycle runtime obligations in section 6 remain material. No OS/device support claim is inferred merely from an exploit eligibility plist, a preprocessor version check or a successful host compile.

### 7.2 Validation executed during this review

**First pass, recorded macOS validation:** the following results were obtained earlier in this review on the user's Mac. They are not presented as freshly rerun in the later Linux sandbox.

| Validation | Result | What it does not prove |
| --- | --- | --- |
| `test_launchctl_compatibility`, `test_crashreporter_compatibility`, `test_jailbreakd_bootstrap`, `test_basebin_linkage`, `test_boomerang_resume` | 22 tests passed | Actual device activation/restart, private ABI correctness or executing the writer/helper code on a target |
| Existing `version_tests.m` linked with current comparator | 10 checks passed | Debian version ordering |
| Existing trust/signature fixture linked with current signature/branding code, using copied bootstrap killall, dash and pam_unix.so | All three fixtures passed, with ordinary and simulated TXM branches | Real trustcache/attachment acceptance, recursive trust, PAM authentication or the newly edited loader implementation |
| New temporary package-version observations plus installed dpkg oracle | Three ordering/equality counterexamples reproduced | Every possible Debian version string |
| New temporary fixture containing the exact nested enumeration helper | Outer count 1 instead of 2; inner count 2 | That a production callback necessarily nests today, or the separate configuration race |
| Actual `exec_trace.m`, arm64e iOS syntax compilation with `-Wshadow` | Compiles; six shadow warnings, including the return-value defect at 276 versus 251 | A real ptrace/exception delivery test; not every shadow warning is a defect |
| Read-only host `task_get_exception_ports` query with different initial count values | Both return success and one record; count hypothesis rejected | All historical OS behavior or target-device exception-port authorization |

Those first-pass outputs and temporary fixtures are under the remote `.build/port-review/evidence/validation/`. Existing packaged binaries were used for package/linkage checks; they were not rebuilt into a fresh Wolf archive. The full host runner was not run in that pass. Subsequent user-uploaded snapshots made the formerly inaccessible code available; they did not supply a macOS runtime or Xcode SDK to the local Linux sandbox.

**Earlier uploaded-snapshot continuations:** PR-022 through PR-042 record the fixtures and observations obtained during those continuations, including layout, hookd state/counts, dispatch matching, donor IDs/groups, trustcache batching, capability consumption and XPF teardown. Those recorded results are not silently counted as fresh reruns in the final continuation. Their mock-versus-device limitations are stated in each entry.

**Recorded lifecycle-pass checks:** five local fixture drivers were executed again in that pass after its findings were recorded. They were not rerun in the later report-reconciliation pass. The C observation programs use AddressSanitizer/UndefinedBehaviorSanitizer; the make queries execute no build recipes. The negative outcomes below are the reproduced defects, not claims that production behavior is correct.

| Fixture | Observed outcome | Scope |
| --- | --- | --- |
| `fork-handshake.py`, all four trees | Failed readiness/ack reads permit progress; an unused local pipe writer prevents EOF until closed | Actual source functions with fork/RPC stubs, plus an ordinary nonblocking pipe observation; no real fork or target process |
| `injector-outcome.py` | Four distinct injection/helper failures still yield CLI status 0 | Actual entry/helper logic with process/Mach stubs; no injection |
| `injector-suspension.py` | Failed remote-call wait returns 5 while one peer suspension remains; successful control balances it | Actual arbCall with suspension/thread-operation stubs |
| `wire-layout.py` | Check-in sizes stay 3,112 while the root moves 49 -> 50; old decoding sees empty strings; trust request grows 88 -> 96 | Actual old/new declarations with an explicit Darwin LP64 primitive-layout model; no Mach traffic or old native caller execution |
| `incremental-graph.py`, four current targets | Newer shared header/library leaves target current; newer listed source requires rebuild | Actual Makefiles and make dependency evaluation in disposable fixtures; no compilation/signing/package recipe |

PR-045 additionally uses source ordering and Apple's documented blocking-alert contract; no notification or daemon job was executed. Fresh evidence is in the local `review-evidence/` directory and the accompanying evidence ZIP. It contains no working copy of PORT_REVIEW.md. No implementation files or production tests were edited, and no device was installed, launched, activated, restarted or modified by this review.

**Supplementary follow-up, fresh validation:** `review-evidence/checks/supplementary-failures.py` was executed twice after its fixtures were finalized. It compiles the actual four upload functions and actual four restart helpers with ASan/UBSan and external operations replaced by explicit non-device stubs. All four allocation-refusal observations lose the old static trustcache; current/upstream-3 successful controls are clean. All four restart helpers overstate a modeled 128-byte buffer as 4,096 bytes; their 64-byte-record and query-refusal controls are clean, and a simulated 512-byte result produces the expected ASan heap-buffer-overflow. Separate expected-failure logs capture the historical static-trustcache copy overread in upstream 2/legacy RootHide, which does not occur in the current/upstream-3 controls. Expected sanitizer failures establish the defects; they are not a claim of sanitizer-clean production behavior. These new results do not rerun or replace the earlier continuation fixtures above.

**Report-reconciliation pass, freshly rerun:** the two previously reported but unsaved findings were independently checked against the uploaded sources and their retained fixture scripts. Both scripts were then recompiled and executed against upstream 3 and the current port with ASan/UBSan:

| Fixture | Observed outcome | Scope |
| --- | --- | --- |
| `trust-fd-identity.py` | No replacement: original object reaches trust/attachment. Path replacement: result 0, but both operations receive the replacement object. | Actual server function; real temporary files/rename; simulated descriptor query and signature/attachment operations; no real cross-process request or kernel signature acceptance. |
| `fcntl-error-domain.py` | RPC failures 5 and 268451843 escape unchanged as nonnegative fcntl returns. Current-port errno remains the earlier EACCES; successful control returns 0. | Actual hook with signature/Mach/RPC stubs; ordinary msyscall_errno/cerror boundary separately inspected; no target syscall interception. |

Both fresh runs completed with no sanitizer errors in the observation fixtures. These are reproduced error-path defects, not successful end-to-end jailbreak tests. The new `port-review-final-evidence.zip` retains this reconciliation pass's fixture drivers, generated C, observations, source hashes and four-way disposition table; it does not claim to contain every earlier runtime's evidence. PR-048/049 now record these results in the authoritative report rather than leaving them only in chat.

### 7.3 Workspace preservation verification

The report-reconciliation pass verifies all 3,790 extracted source files against hashes captured before its analysis: zero modified, missing or added files. Counts are again 772/1,009/870/1,139 for upstream 2/upstream 3/legacy/current, and all four original ZIP hashes are unchanged. No local PORT_REVIEW.md exists. Only the live report is edited through Codexify. A requested remote shell consistency check was blocked and is not claimed to have run; it was not rerouted through another command executor. Successful patch responses and structured report readback establish the saved edits, separately from local source-preservation validation.

After that fresh run, the 3,790 extracted source files (772/1,009/870/1,139 by upstream 2/upstream 3/legacy/current) were checked against this follow-up's pre-analysis hashes: zero changed, missing or added source files. The four original ZIP SHA-256 values remain unchanged. A recursive sandbox search finds no PORT_REVIEW.md. The latest Codexify status still lists the same 23 pre-existing paths/statuses; all report edits were made through Codexify. The supplementary evidence archive contains only this follow-up's scripts, generated C, logs, manifests and preservation results, not a local report or a claim to contain evidence from earlier runtimes.

The first pass's remote byte-comparison command did not complete; that historical result is not retroactively claimed as successful. In the uploaded-snapshot continuation, the extracted sources were hashed before analysis and verified again afterward: all 772 upstream-2, 1,009 upstream-3, 870 legacy-RootHide and 1,139 current-port source files match their initial hashes, with zero added, missing or modified source files. ZIP manifests/disposition metadata and deliberately excluded report/symlink entries account for the different archive-member totals. No local PORT_REVIEW.md exists, and the original four ZIPs are preserved.

The latest Codexify Git status contains the same 23 paths as the earlier workspace status, including the pre-existing Wolf overrides, experiments, coordination files and the untracked report. All edit operations in this continuation target only the live PORT_REVIEW.md through apply_patch; all new fixtures live outside the source trees in the local evidence directory. Git status is a path/status comparison, not a byte-for-byte proof of every remote uncommitted file. No implementation fix, reset, commit, push or device operation was performed.

## 8. Review log

### Permission-boundary continuation - 2026-09-15

Continued in the same 142-path starting worktree. Added `tests/test_mobile_permissions.py` and `tests/mobile_permissions_tests.m`, observed the failing baseline, and repaired only the ordinary permission-traversal implementation in `BaseBin/libjailbreak/src/util.m`. The initial 23 cases and expanded 37 cases passed after repair; the complete utility translation unit passed iOS-15-minimum syntax checks for both arm64 and arm64e. The existing host runner automatically discovers the new Python test. PR-004 records the exact evidence and remaining root-transition obligations; its checkbox remains open. PR-014's second variadic interception site was clarified without changing that implementation. No previously resolved finding or validation gap was newly checked or reopened. The snapshot now has 23 recorded resolutions and 26 open findings, split into seven with recorded work and nineteen pending.

An initial read-only shell command to extract report status was blocked before execution and was not retried. Independent new filesystem tests and syntax checks subsequently executed normally through Codexify. No local ChatGPT sandbox source/file access, production build, device operation, commit or push was performed. Exploit, privilege-escalation, injection and platform-protection-bypass mechanisms were not modified. Scoped post-edit verification is recorded only when actually completed; preceding historical blocked results are not recovered or reclassified as successful.

The subsequent `PYTHONPATH=tests python3 -m unittest -v test_mobile_permissions test_port_review_tracking` command started, but its result retrieval was blocked. The result remains unknown and was not retrieved through another route or replaced with split reruns. The two completed permission-suite results and the two-architecture syntax check above remain the fresh evidence; no combined-suite or post-edit report-checker pass is claimed. Report readback and the final aggregate diff establish the saved edits separately from execution results. A progress message was saved to Codexify chat but its notification failed; the message was not resent merely to retry delivery.

### Command-tool continuation - 2026-09-15

Rediscovered callable native `exec_command` and `apply_patch` tools and resumed the already-bound worktree without creating another. The starting worktree contained 142 changed paths; prior fixes, Wolf overrides and unrelated work were preserved. Fresh report-only verification passed all 30 tests, including the existing exact-heading-boundary repair. PR-041's existing receiver and parent repairs passed 14 and 25 actual-source scenarios respectively with fatal ASan/UBSan diagnostics. Only after both successful runs was PR-041 checked off, and the snapshot was reconciled to 23 resolved and 26 open findings. The ten validation gaps remain open. These are fresh isolated host checks, not recovery of earlier blocked command output or device/private-ABI qualification. All file and command access uses Codexify; no local ChatGPT sandbox access, production build, device operation, commit or push has occurred in this continuation.

The subsequent combined command for `test_port_review_tracking`, `test_fixture_sanitizers`, `test_jbctl_barrier`, `test_jbctl_launch`, `test_entitlement_blob` and `test_entitlement_blob_host` was blocked before execution. It was not rerouted or split into replacement runs. Thus the three preceding individual passing commands remain the fresh evidence; no combined-regression success, new entitlement-parser verification or post-edit live-report test is claimed. The progress-notification call was also blocked after the successful report edit. PR-028 remains open for its recorded debugger-positive and target-iOS coverage gaps. This continuation changed only this report, not production sources or tests.

### Utility verification and size-probe repair - 2026-09-15

The execution/edit tools were rediscovered successfully in the existing worktree. PR-011's existing package-selection repair passed its two unittests, and the independent app-version suite passed all ten checks. PR-047's existing buffer repair passed the original eight scenarios and an expanded 20-scenario ownership/capacity fixture; both findings were checked only after those completed results. PR-027's seven-case parser suite independently passed here while another writer recorded its closure. A final combined run of those four suites completed with all five unittests passing. Concurrent report edits were preserved; the user explicitly authorized multiple writers on different findings.

The report checker received an anchored heading-boundary correction and an inline-reference regression. Its test command was blocked before execution, so no passing checker result is claimed or obtained through another route. PR-028 then received the actual self-process integration tests and narrow preliminary-header repair recorded above, with two failing modeled cases and a failing real control observed before the successful two-suite run. No debugger entitlement was granted, no other process was inspected or modified, and no device operation, production package build, commit or push occurred. All source/report/test access and edits used Codexify; no local ChatGPT sandbox source or file was accessed. The remaining PR-028 target/private-entitlement qualification and all ten validation gaps stay open.

After the final parser/test changes, `PYTHONPATH=tests python3 -m unittest -v test_package_selection test_app_versions test_restart_process test_dyld_uuid test_entitlement_blob test_entitlement_blob_host` passed all seven unittests (12.084 seconds, exit 0). This scoped run covers the 32 package cases plus metadata consistency, ten app-version checks, 20 process-buffer scenarios, seven UUID cases, 11 modeled entitlement cases and two real self-process controls. It does not include the blocked report checker or certify the complete production tree.

### Binary UUID verification and concurrent report merge - 2026-09-15

PR-027's existing bounded UUID comparison passed its seven-case actual-source fixture with fatal sanitizer diagnostics and was checked off. This chat's newly verified closures are PR-002, PR-012, PR-016, PR-017, PR-026, PR-027 and PR-042. No additional production code was changed for PR-027.

Two report patches failed their expected-context checks while another utility continuation recorded passing PR-011 and PR-047 results and expanded the report checker to 30 cases. Those intervening changes were preserved and PR-027 was merged against the current text. PR-011/PR-047 results belong to that other continuation: their commands were blocked in this chat. This chat's report-only checker and Git diff/whitespace-check commands were also blocked, with no reroute or passing claim. The merged snapshot records 22 resolved and 27 open findings; all ten separate validation gaps remain open.

### Defensive protocol continuation - 2026-09-15

Resumed the exact existing worktree without allocation and read all 948 report lines through Codexify, including a separate reread of the truncated span. The initial status listed 133 changed paths. Existing Wolf overrides and unrelated changes were not edited; no ChatGPT sandbox files were accessed.

PR-011's existing package/version implementation and PR-047's existing argument-buffer repair were inspected, but their isolated host-test commands were blocked before execution. Both findings remain unchecked. PR-026's separate client and server memory-safety/ownership repairs completed the failing-then-passing fixture runs recorded above. The existing PR-002 repair passed seven identity cases; PR-012 passed 20 configuration cases; PR-016 passed six tests covering 21 dependency cases; PR-017 passed 34 argument-contract and 28 diagnostic cases; PR-042 passed 283 teardown cases. Configuration and argument-contract suites passed again after enabling fatal sanitizer diagnostics; the teardown suite also used fatal diagnostics. PR-002, PR-012, PR-016, PR-017, PR-026 and PR-042 are newly checked in this continuation; all ten validation gaps remain open. The report-only checker was adjusted for neutral resolution wording, with two additional tests, but its execution was blocked and is not claimed as verified. Changes were limited to protocol bounds, allocation cleanup, tests and report bookkeeping; the executable-memory operation implementations and caller-admission rule were left untouched. No device operation, native package build, commit or push was performed.

The safe blocked commands from this continuation were posted in Codexify chat for later user verification: the PR-011 and PR-047 host suites, the PR-026 Git diff/whitespace check, and the report-only checker suite. None was rerouted or counted as successful.

### Utility and tracking continuation - 2026-09-15

Resumed the same worktree through Codexify and inspected the ordinary app/Debian version utilities, their callers and the existing package-selection fixture. No production source was changed. A fresh compile/run of the standalone `tests/version_tests.m` against only `NSString+Version.m` started with Foundation and sanitizers, but retrieving its result was blocked. No build or test success is claimed, and the blocked result was not recovered through another route. PR-011 and every other finding retain their preceding status.

Added the two report-only checker files described above. The first `python3 -m unittest discover -s tests -p test_port_review_tracking.py -v` run completed against the deliberate no-op validator: 27 tests, 26 expected assertion failures, exit 1. That result establishes the test baseline, not a defect count in the report. After implementing the validator, the combined post-change test/checker command was blocked before execution. Its passing behavior and current-report result remain unverified; neither command was rerouted or replaced with another execution of the same checks. No finding or validation checkbox was ticked. This continuation accessed no ChatGPT sandbox file and performed no device operation, commit or push.

### Tracking reconciliation - 2026-09-15

Resumed the exact existing worktree through Codexify without creating another worktree and read all 903 current report lines, including a separate reread of the truncated span. The initial Git status contained 131 changed paths. A read-only document check confirmed 49 uniquely ordered findings with exactly one resolution checkbox each, 14 previously checked entries, 15 open entries with recorded implementation or fixture work, 20 pending entries and ten validation-matrix rows.

Reopened PR-049 solely because PR-034 explicitly records later unverified changes affecting its hook and regression fixture, retaining the prior successful evidence. Added ten unchecked validation-gap items and the progress snapshot above. This continuation changes only the report and does not claim any new runtime repair, production-test result, build, device operation, commit or push. Existing source changes and private Wolf overrides are left untouched; no ChatGPT sandbox sources or files are accessed. Previously blocked verification is not rerouted. Exploit, privilege-escalation, injection and platform-protection bypass improvements are outside the implementation work performed in this continuation; the corresponding unresolved items remain open.

A read-only Python check of the live Markdown completed with exit 0 after these edits. It verified the ordered PR-001 through PR-049 IDs, one resolution checkbox per finding, the exact thirteen retained checked IDs, 36 open findings split into sixteen with recorded work and twenty still pending, ten uniquely matched unchecked validation gaps, the summary-table counts and preserved historical PR-049 evidence. No new checkbox was ticked. This is document-consistency verification only, not a production regression or target-qualification result.

### Implementation phase - 2026-09-14

Resumed `/Volumes/data/.codex/worktrees/190a/Dopamine2-roothide` without creating a worktree. Read all 789 lines of the authoritative report end-to-end through Codexify before editing. Added 49 unchecked resolution items. All source inspection, edits, commands and verification for this phase use Codexify; no ChatGPT sandbox files are accessed. The initial status has 24 changed paths, including the pre-existing implementation experiments, private Wolf overrides, coordination files and report. Per-finding entries below are updated only after actual verification.

On the latest implementation continuation, all 895 current report lines were read through Codexify. The live report already contained 49 resolution checkboxes and eight checked entries (PR-003/019/022/030/036/037/038/039), beyond the older saved plan. The initial 71-path working state is preserved, including prior fixes, private Wolf overrides and unrelated files. Previously blocked verification results are not treated as successful or retrieved through alternate routes. Further fixes and their fresh verification are recorded individually in section 4.

On this continuation, all 897 report lines were read through Codexify, including a separate reread of a truncated middle span. The live report has 12 checked findings, ahead of the saved 10-finding plan, and the initial working state contains 103 changed paths. The exact existing worktree was resumed without allocation of another worktree. PR-017's additional argument-slot defect, repairs, fresh failing regressions and blocked post-fix verification are recorded above; no ChatGPT sandbox source/file access was used.

### Uploaded-snapshot continuation

On the report-reconciliation continuation, all 750 lines of the live report were read through Codexify, including PR-046/047. Codexify's write tools were rediscovered and successfully used. The four uploaded source ZIPs match their recorded SHA-256 values and pass every member CRC check. The sandbox report was again the original 67,177-byte baseline, byte-identical to the archived copy, with no local edits to merge; it was deleted. The two findings previously described only in chat were rechecked, freshly reproduced and saved as PR-048/049. The live report now contains 49 entries and distinguishes this pass's fresh observations from historical validation. Only this live report was edited; no implementation changes were made.

On the supplementary follow-up, all 718 lines of the existing live report were read through Codexify before editing. The four uploaded ZIP SHA-256 values and all member CRCs were verified again. The sandbox again contained only the original 67,177-byte report, byte-identical to the archived baseline; there were no local edits to merge. That stale copy was deleted, and extraction omitted archived report copies. The supplementary cross-path consistency pass added PR-046/047 to the existing 45-entry inventory, recorded the independently corrected upstream trustcache overread, reran the new actual-source fixtures and verified source/ZIP preservation. The resulting 47-entry source inventory is complete for its stated scope; implementation and device qualification remain separate, unperformed phases.

On the latest continuation, all 641 lines of the live report were read through Codexify. The four ZIP checksums and every member's CRC were checked again locally. A newly present sandbox report was again byte-identical to the original 67,177-byte archived baseline, with no local edits to merge; it was deleted. Extraction again omits PORT_REVIEW.md. Only the live Codexify report is edited. The changed-file inventory now includes recursively expanded submodules (429 upstream-2-to-3 paths, 229 upstream-2-to-legacy paths, 254 upstream-3-to-current paths); those counts differ from the earlier top-level Git diff counts by scope, not newly changed upstream revisions.

The complete live report (all 399 original lines) was read through Codexify before the first uploaded-snapshot continuation. All four user-uploaded ZIPs were present in the local analysis sandbox and had the expected SHA-256 values: current-port `5420a4c4616c0ff97d8ce7884e753d91beb8f3f50df45381c9d512d935862197`, dopamine2 `4202a96ba2da922274ae6a13bd831619b59df3b0d7a987f1f86cff4ec84c5ab0`, dopamine3 `29d65aed67aee955fd3638306f896c8a53df14988910a2d6b9f5df9377026e69`, roothide2 `91837e64639176fb857169ce013e8433f7c4eac39b16af5cfd6f2d3d003879af`. Their embedded SNAPSHOT.json files retain the repository/submodule pins and dirty-source scope. Extraction includes the actual uncommitted loader implementation, not a substitute from upstream. No Codexify file-export/materialization operation is used for these continuations. Only the authoritative report on the user's machine is edited, through Codexify; the stale local report was subsequently checked and deleted as recorded below.

The old blocked-read gaps describe the first pass, not permanent access restrictions on manually supplied files. They have now been replaced with source findings/counter-evidence and the remaining runtime obligations; successful reads alone do not establish runtime correctness.

On the next continuation, all 499 lines of the live report were read through Codexify. Its write tools were rediscovered successfully after the service restart. The sandbox copy of PORT_REVIEW.md is exactly the original 67,177-byte exported report (SHA-256 `7cfb8ded95f5e094442cb6a65013b4af8c6e3c77b5c62d968c06d0dd22197cbd`) and byte-identical to the report inside current-port.zip: it contains no subsequent local edits to merge. The live report already contains PR-022 through PR-030 and remains authoritative. The stale local copy is removed, and extraction omits archived PORT_REVIEW.md files so they cannot accidentally become working reports. The four ZIPs remain unchanged.

- Established the exact existing workspace and initial Git state. Recovered previous investigation notes without treating them as verified conclusions.
- Identified that the legacy common ancestor differs from the current upstream 2 branch. Both comparisons are required; a common-base-to-3 diff includes intervening upstream 2 work and must not be mislabeled entirely as 2-to-3 architectural change.
- Cloned all three reference branches independently and confirmed that their heads match the recorded pins. Created complete pairwise diff/path inventories.
- Added findings progressively while tracing lifecycle and subsystem interactions. Corrected the initial non-ad-hoc trust hypothesis, the XPC borrowed-reference hypothesis and the exception-count hypothesis when counter-evidence appeared.
- Executed the selected existing regressions and isolated host observations above. Kept new fixtures/evidence in the ignored audit directory rather than modifying the production test suite.
- The first pass consolidated 21 numbered findings/risks and a 16-boundary architecture matrix. Uploaded-source passes expanded that inventory to 42, the lifecycle pass to 45, and supplementary/reconciliation work to 49. The count includes inherited defects and conditional risks, not 49 newly introduced regressions or 49 demonstrated device failures.
- The lifecycle pass added PR-043/044/045, expanded PR-023's native/wire ABI evidence, reproduced PR-016's actual prerequisite behavior and reran its five local fixture drivers. Subsequent work added PR-046/047 and an upstream correction; this reconciliation saved the two remaining reproduced findings as PR-048/049, updated the architecture/validation record and verified local source preservation. The source-inventory phase has no remaining unsaved findings from these passes; implementation and target qualification remain separate work.
